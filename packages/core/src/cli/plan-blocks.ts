/** Compatibility re-export for the canonical recap CLI implementation. */
export {
  DEFAULT_PLAN_APP_URL,
  defaultPlanBlocksOut,
  fetchPlanBlockCatalog,
  normalizePlanBlockFormat,
  planActionEndpoint,
} from "@agent-native/recap-cli";
export type {
  FetchPlanBlockCatalogInput,
  FetchPlanBlockCatalogResult,
  PlanBlockFormat,
} from "@agent-native/recap-cli";
