---
title: "Cloudflare"
description: "일치하는 Nitro 프리셋으로 agent-native 앱을 Cloudflare Pages나 Cloudflare Workers에 배포하세요."
---

# Cloudflare

## Cloudflare Pages {#cloudflare-pages}

```ts filename="vite.config.ts"
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_pages" } })],
});
```

## Cloudflare Workers {#cloudflare-workers}

Cloudflare Worker에는 Nitro의 네이티브 모듈 출력을 사용하세요. 이는 SSR을
계속 활성화한 상태로 유지하며 Worker 진입점과 정적 자산을
`.output/server` 및 `.output/public`에 생성합니다:

```ts filename="vite.config.ts"
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_module" } })],
});
```

생성된 Worker를 Wrangler로 빌드하고 배포하세요:

```bash
pnpm build
npx wrangler deploy --config .output/server/wrangler.json
```

`NITRO_PRESET=cloudflare_module`로 프리셋을 명시적으로 선택할 수도 있습니다.
배포 전에 내구성 있는 외부 PostgreSQL 데이터베이스를 구성하세요. Worker 파일 시스템은
영구적인 애플리케이션 저장소가 아닙니다.

## 다음 단계

- [**Deployment**](/docs/deployment) — 첫 프로덕션 배포 전에 영구 데이터베이스와 프리셋을 선택하세요
- [**Workspace Deployment**](/docs/workspace-deployment) — Cloudflare Pages는 통합 `deploy` 명령의 기본 프리셋입니다
- [Node.js](/docs/node-js), [Docker](/docs/docker), [**Vercel**](/docs/vercel), [**Netlify**](/docs/netlify), [**Other Platforms**](/docs/other-platforms) — 그 외 Nitro 프리셋들
