---
title: "다중 앱 작업 공간 속 클립"
description: "클립이 Dispatch 및 다른 앱과 연결되는 방식, 에이전트가 읽을 수 있는 클립, Brain으로 내보내기, CRM 통화 증거, 자연어 위임을 다룹니다."
---

# 다중 앱 작업 공간 속 클립

클립은 [Dispatch](/docs/template-dispatch) 작업 공간에 속한 여러 앱 중 하나입니다.
이 페이지는 그것이 실제로 무엇을 의미하는지, 즉 연결이 어떻게 동작하는지, 그리고
다른 앱과 에이전트가 여러분의 클립에 실제로 어떻게 작용하거나 클립에서 일어나는 일에
반응할 수 있는지를 구체적으로 다룹니다. 전체 개요는 [클립](/docs/template-clips)
문서를 참고하세요.

## 클립이 Dispatch와 연결되는 방식

이것이 자동으로 이루어지는지는 클립과 Dispatch를 어떻게 함께 설정했는지에 따라
달라지며, 둘 중 하나가 호스팅되어 있는지 로컬에서 실행 중인지와는 관련이 없습니다.

### 같은 작업 공간: 자동

클립과 Dispatch가 하나의 Agent-Native 작업 공간에 (`create` 또는 `add-app`을
통해) 함께 생성되거나 추가되어 하나의 단위로 배포되었다면, 이미 하나의 `.env`,
하나의 `A2A_SECRET`, 하나의 작업 공간 레지스트리를 공유합니다. 검색과 인증된 A2A
호출 모두 그 작업 공간이 여러분의 컴퓨터에서 실행되든 프로덕션에서 실행되든 별도로
설정할 것 없이 그대로 동작합니다. 클립을 기반으로 한 실제 다중 앱 제품이라면 이
설정이 기본적으로 예상됩니다.

### 별도 배포: 수동 단계 하나

클립과 Dispatch가 독립적으로 생성된 두 개의 Agent-Native 프로젝트라면, 시크릿을
공유하지 않으므로 아직 서로를 호출할 수 없습니다. 이들을 연결하려면 양쪽 각각에서
수동 단계 하나가 필요합니다. 설정 → 연결된 에이전트, Agent 페이지의 연결 탭, 또는
Dispatch → 에이전트 → 외부 에이전트 추가에서 공유 시크릿과 함께
`remote-agents/<id>.json` 리소스를 추가하세요. 전체 과정은
[A2A 프로토콜](/docs/a2a-protocol) 문서를 참고하세요.

어느 쪽 상황인지 확인하려면 두 앱 중 어느 쪽 에이전트에게든 "어떤 앱들을 볼 수
있어?"라고 물어보세요. Dispatch의 답변에 클립이 빠져 있다면 두 번째 경우에
해당합니다.

## 통합 전략

### 에이전트가 읽을 수 있는 클립

공개 클립 공유 링크를 에이전트에 붙여넣거나, 비공개 클립이라면 **에이전트와 공유**
행의 **공유 → 복사**를 사용하세요. 어느 쪽이든 에이전트는 두 시간 뒤에 만료되는
링크를 사용해 동영상을 다운로드하지 않고도 전사를 읽고 핵심 프레임을 볼 수 있습니다.

이미지를 볼 수 있는 대부분의 에이전트는 ChatGPT, Claude Code, Cursor, Codex를
포함해 이를 스스로 처리합니다. 일부 텍스트 전용 채팅은 전사는 읽을 수 있지만
프레임은 읽지 못하므로, 이런 경우에는 이미지 지원 에이전트에서 클립을 여세요.

| Endpoint                                          | What agents get                                                                                         |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `/api/agent-context.json?id=<recordingId>`        | 클립 메타데이터, 전사 상태, 챕터, CTA, 추천 프레임, 전사/프레임 API 링크                                |
| `/api/agent-transcript.json?id=<recordingId>`     | `startMs`, `endMs`, 읽을 수 있는 타임스탬프, 텍스트, 선택적 소스 라벨이 포함된 타임스탬프 전사 세그먼트 |
| `/api/agent-frame.jpg?id=<recordingId>&atMs=<ms>` | 원본 동영상 타임스탬프에서 추출한 JPEG 프레임                                                           |

이 엔드포인트들은 공유 페이지와 동일한 공개/비밀번호/만료 규칙을 따르거나, 임시
에이전트 링크의 범위가 지정된 `agent_access` 토큰을 받습니다. 비밀번호로 보호된
클립은 비밀번호를 한 번만 요구합니다. 성공적인 응답은 단기 토큰화된 링크를 반환하므로
이후 에이전트가 평문 비밀번호를 알 필요는 없습니다.

<Endpoint id="doc-block-clips-agent-context" title="에이전트 컨텍스트 진입점" method="GET" path="/api/agent-context.json" summary="공유된 클립에 대한 간결하고 에이전트가 읽을 수 있는 설명" auth="Same public / password / expiry rules as the share page, or a scoped agent_access token" params={[
  {
    "name": "id",
    "in": "query",
    "type": "string",
    "required": true,
    "description": "녹화 ID"
  },
  {
    "name": "agent_access",
    "in": "query",
    "type": "string",
    "required": false,
    "description": "에이전트 링크용 복사에서 받은 임시 토큰"
  }
]} responses={[
  {
    "status": "200",
    "description": "클립 메타데이터, 전사 상태/failureReason/retryAfterSeconds, 에이전트 안내, 전사/프레임 API 링크"
  }
]}>

클립 메타데이터, 전사 상태, 챕터, CTA, 추천 프레임, 전사 및 프레임 API 링크를 반환합니다. 공개 공유 페이지와 임시 비공개 에이전트 링크에 안내되어 있어, 텍스트 전용 또는 이미지 전용 에이전트도 원본 동영상을 수집하지 않고 녹화를 이해할 수 있습니다.

</Endpoint>

<Endpoint id="doc-block-clips-agent-transcript" title="타임스탬프가 있는 전사" method="GET" path="/api/agent-transcript.json" summary="공유된 클립의 타임스탬프 전사 세그먼트" params={[
  {
    "name": "id",
    "in": "query",
    "type": "string",
    "required": true,
    "description": "녹화 ID"
  }
]} responses={[
  {
    "status": "200",
    "description": "전사 상태, failureReason, retryAfterSeconds, 전체 텍스트, 그리고 startMs, endMs, 읽을 수 있는 타임스탬프, 텍스트, 선택적 소스 라벨이 포함된 세그먼트"
  }
]}>

</Endpoint>

<Endpoint id="doc-block-clips-agent-frame" title="타임스탬프의 프레임" method="GET" path="/api/agent-frame.jpg" summary="원본 동영상 타임스탬프에서 추출한 JPEG 프레임" params={[
  {
    "name": "id",
    "in": "query",
    "type": "string",
    "required": true,
    "description": "녹화 ID"
  },
  {
    "name": "atMs",
    "in": "query",
    "type": "integer",
    "required": true,
    "description": "밀리초 단위의 원본 동영상 타임스탬프"
  }
]} responses={[
  {
    "status": "200",
    "description": "image/jpeg 프레임"
  }
]}>

</Endpoint>

전사가 아직 준비되지 않았다면, 예의 바른 에이전트는 바로 포기하는 대신 잠시 기다렸다가 다시 시도합니다. 긴 녹화는 몇 분이 걸릴 수 있습니다. Builder 크레딧이 소진되어 전사가 실패했다면, 에이전트는 그렇게 말하고 [전사 및 AI 메타데이터](/docs/template-clips-features#transcription-and-ai-metadata) 문서를 안내해야 합니다.

### Brain으로 내보내기

작업 공간이 Brain을 연결하면, 클립은 모든 녹화가 끝날 때마다 준비된 전사를 자동으로 전송합니다. 새 녹화가 하나씩 들어오길 기다리는 대신, 예를 들어 지난 28일 치 전체처럼 이전 녹화를 한꺼번에 채워 넣도록 에이전트에게 요청할 수도 있습니다. 에이전트는 몇 개의 전사가 내보내졌는지, 건너뛰었는지, 플래그가 지정되었는지 보고합니다.

### CRM 통화 증거

CRM 도구는 때때로 전체 녹화 없이 통화가 있었다는 증거만 필요로 합니다. 클립은 동영상이나 전사가 아니라 불투명한 클립 ID와 지속되는 링크만 건네줍니다. 이는 여러분이 이미 볼 수 있는 녹화에서만, 그리고 여러분이 요청하거나 승인된 자동화가 트리거될 때만 동작합니다.

<Diagram id="doc-block-clips-crm" title="CRM 통화 증거" summary={"clip.created 자동화가 미디어 자체가 아니라 지속되는 링크를 CRM 워크플로에 건네줍니다."}>

```html
<div class="diagram-flow">
  <div class="diagram-node">녹화 준비 완료</div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-box" data-rough>
    clip.created<br /><small class="diagram-muted">승인된 자동화</small>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-panel center">
    <span class="diagram-pill accent">증거 링크</span
    ><small class="diagram-muted">id + /r/&lt;id&gt;</small>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-box">CRM 레코드</div>
</div>
```

```css
.diagram-flow {
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
}
.diagram-flow .center {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}
.diagram-flow .diagram-arrow {
  font-size: 18px;
  line-height: 1;
}
```

</Diagram>

### Dispatch를 통한 자연어 위임

형제 앱이 클립과 함께 동작하는 데 특별한 통합이 필요하지는 않습니다. 여러분이 채팅에서 하는 것과 같은 방식으로 "가격에 대해 논의한 클립을 찾아줘" 같은 자연어 요청을 클립의 에이전트에게 그대로 건네면, 클립이 직접 답하거나 처리합니다.

### 분리된 상태 유지하기

클립은 Brain이나 CRM의 자격 증명을 알아야 할 필요가 전혀 없으며, 반대 방향도 마찬가지입니다. `clip.created` 이벤트와 에이전트가 읽을 수 있는 링크가 앱들 사이에 자리 잡고 있으므로, 클립은 누가 듣고 있는지 알 필요 없이 그저 이벤트를 발생시키고 요청에 답하기만 하면 됩니다.

## 다음 단계

- [**클립**](/docs/template-clips) — 개요와 대체하는 것
- [**기능**](/docs/template-clips-features) — 라이브러리, 나와 공유된 항목, 스페이스, 그 외 모든 기능
- [**Agent와 대화**](/docs/template-clips-agent) — 작업별 프롬프트와 에이전트가 화면을 보는 방식
- [**개발자 가이드**](/docs/template-clips-developers) — 빠른 시작, 액션, 데이터 모델, 라우트
- [**Dispatch**](/docs/template-dispatch) — 이 패턴들이 동작하는 기반이 되는 작업 공간 제어 플레인
- [**자동화**](/docs/automations) — `clip.created`에 반응하는 자동화 선언하기
- [**A2A 프로토콜**](/docs/a2a-protocol) — 앱들이 서로 발견하고 인증하고 호출하는 방법
- [**다중 앱 작업 공간**](/docs/multi-app-workspace) — 앱을 중첩시키는 대신 자사 앱들을 조합하는 일반적인 패턴
