---
title: "الشرائح: دليل المطور"
description: "نموذج بيانات الشرائح، ونموذج الاستمرارية، وقائمة الإجراءات الكاملة، والمسارات، وأين تُجرى التغييرات الشائعة عند تخصيص القالب أو توسيعه."
---

# دليل المطور

هذه الصفحة لأي شخص يخصص قالب الشرائح أو يوسّعه: نموذج بيانات SQL، وكيف تُحفظ
سلامة الكتابات المتزامنة، وكل إجراء يمكن للوكيل استدعاءه، وخريطة المسارات،
وأين تُجرى التغييرات الشائعة.

## البدء السريع

```bash
npx @agent-native/core@latest create my-slides --standalone --template slides
```

راجع [بدء الاستخدام](/docs/getting-started) لمعرفة بقية خطوات الإعداد (`cd`، `pnpm install`، `pnpm dev`) وأساسيات إطار العمل.

## نموذج الاستمرارية

كل مجموعة هي كائن JSON واحد في `decks.data`. تمر كل عملية كتابة عبر إجراء من
نوع قراءة-تعديل-كتابة على الخادم يحتفظ بقفل خاص بكل مجموعة، لذا لا تتلف
الكتابات المتزامنة أبدًا عمل بعضها، سواء كانت من إنسان والوكيل، أو من
إنسانين، يعدّلان شرائح مختلفة من نفس المجموعة.

- **إجراءات الوكيل** (`add-slide`، `update-slide`) تستخدم إجراءاتها الخاصة الدقيقة المخصصة وتتشارك في نفس القفل.
- **محرر المتصفح** يُدرج عمليات دقيقة (`patch-slide`، `delete-slide`، `reorder-slides`، `add-slide`، `patch-deck-fields`) في قائمة انتظار عبر `patch-deck` بدلاً من عملية PUT لكل المجموعة.

<Callout id="doc-block-slides10" tone="warning">

إذا كنت توسّع مسار الحفظ في المحرر، أدرج عملية جديدة في قائمة الانتظار عبر
`patch-deck`. لا تُضِف عملية PUT جديدة لكل المجموعة. فذلك يعيد سباق "آخر كتابة
تفوز" الذي وُجد القفل لمنعه.

</Callout>

## نموذج البيانات

تعيش كل بيانات المجموعات في SQL عبر Drizzle ORM. المخطط: `templates/slides/server/db/schema.ts`.

<DataModel
  id="doc-block-slides7"
  title="نموذج بيانات الشرائح"
  summary={
    "تملك المجموعة شرائحها كـ JSON في decks.data. تتفرع منها التعليقات والإصدارات والمشاركات وأنظمة التصميم."
  }
  entities={[
    {
      id: "decks",
      name: "decks",
      note: "تعيش الشرائح كـ JSON في data، مع ownableColumns",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
          note: "مثال: deck-1712345-abc",
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "data",
          type: "text",
          note: "JSON: { title, slides: [{ id, content, layout }] }",
        },
        {
          name: "design_system_id",
          type: "text",
          nullable: true,
        },
        {
          name: "created_at",
          type: "text",
        },
        {
          name: "updated_at",
          type: "text",
        },
      ],
    },
    {
      id: "slide_comments",
      name: "slide_comments",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
        },
        {
          name: "deck_id",
          type: "text",
          fk: "decks.id",
        },
        {
          name: "slide_id",
          type: "text",
          note: "الشريحة التي يعيش عليها التعليق",
        },
        {
          name: "thread_id",
          type: "text",
          note: "الترابط",
        },
        {
          name: "parent_id",
          type: "text",
          nullable: true,
        },
        {
          name: "content",
          type: "text",
        },
        {
          name: "quoted_text",
          type: "text",
          nullable: true,
        },
        {
          name: "author_email",
          type: "text",
        },
        {
          name: "author_name",
          type: "text",
          nullable: true,
        },
        {
          name: "resolved",
          type: "boolean",
        },
      ],
    },
    {
      id: "deck_versions",
      name: "deck_versions",
      note: "لقطات في وقت محدد للاستعادة",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
        },
        {
          name: "deck_id",
          type: "text",
          fk: "decks.id",
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "data",
          type: "text",
          note: "كائن JSON الكامل للمجموعة",
        },
        {
          name: "change_label",
          type: "text",
          nullable: true,
        },
      ],
    },
    {
      id: "design_systems",
      name: "design_systems",
      note: "رموز علامة تجارية قابلة لإعادة الاستخدام، مع ownableColumns",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "description",
          type: "text",
          nullable: true,
        },
        {
          name: "data",
          type: "text",
          note: "الألوان / الطباعة / التباعد",
        },
        {
          name: "assets",
          type: "text",
          nullable: true,
        },
        {
          name: "custom_instructions",
          type: "text",
        },
        {
          name: "is_default",
          type: "boolean",
        },
      ],
    },
    {
      id: "deck_share_links",
      name: "deck_share_links",
      note: "لقطات روابط مشاركة عامة محفوظة بشكل دائم",
      fields: [
        {
          name: "token",
          type: "text",
          pk: true,
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "slides",
          type: "text",
          note: "لقطة JSON للشرائح",
        },
        {
          name: "aspect_ratio",
          type: "text",
          nullable: true,
        },
        {
          name: "created_at",
          type: "text",
        },
      ],
    },
  ]}
  relations={[
    {
      from: "decks",
      to: "slide_comments",
      kind: "1-n",
      label: "التعليقات",
    },
    {
      from: "decks",
      to: "deck_versions",
      kind: "1-n",
      label: "اللقطات",
    },
    {
      from: "design_systems",
      to: "decks",
      kind: "1-n",
      label: "مُطبَّق على",
    },
  ]}
/>

تُعيّن جداول المشاركة الخاصة بإطار العمل (`deck_shares`، `design_system_shares`) الجهات (مستخدمين أو مؤسسات) إلى أدوار مشاهد/محرر/مسؤول لكل مورد، وتُنشأ عبر المساعد المشترك `createSharesTable` بدلاً من كتابتها يدويًا.

#### بنية الشريحة

كل شريحة داخل `decks.data` هي:

```json
{
  "id": "slide-1",
  "layout": "title",
  "content": "<div class=\"fmd-slide\" style=\"...\">...</div>"
}
```

`content` هو HTML خام. يوفّر العارض نسبة العرض إلى الارتفاع الثابتة والخلفية،
ويوفّر HTML كل ما بداخلها. يُدعم التضمين الغني أيضًا: يمكن أن تعيش رسوم
تخطيطية مرسومة باليد ومخططات بيانية مُعرَّضة مباشرة داخل شريحة إلى جانب HTML
عادي.

## قائمة الإجراءات

### دورة حياة المجموعة

- `create-deck` — مجموعة جديدة، مع تعبئة مسبقة اختيارية بالشرائح، أو استبدال مجمّع وذري لشرائح مجموعة موجودة. حذف `slides` أو تمرير `[]` لإنشاء مجموعة فارغة. يفتح الإجراء المجموعة الجديدة من خلال حالة التطبيق.
- `get-deck` — كائن JSON الكامل للمجموعة، أو عرض ملخّص مضغوط للشرائح
- `list-decks` — كل مجموعة يمكن للمستدعي رؤيتها، مع عروض خفيفة/مضغوطة/كاملة للشرائح لاستقصاء رخيص التكلفة
- `duplicate-deck` — نسخ مجموعة بمعرّفات شرائح جديدة
- `update-deck-aspect-ratio` — 16:9 أو 1:1 أو 9:16 أو 4:5

تدعم `add-deck` و`save-deck` و`delete-deck` مسارات الإنشاء المتفائل
والتراجع/الإعادة والحذف في محرر المتصفح. يستخدم الوكيل بدلاً منها
`create-deck`، و`add-slide`/`update-slide`/`patch-deck`، وإجراءات المشاركة
الخاصة بإطار العمل.

### طلبات الوصول

- `request-deck-access`، `approve-deck-access-request`، `get-deck-access-status` — مسار طلب الوصول لمجموعة خاصة: يطلبه زائر لا يملك صلاحية وصول، ويوافق المالك عليه أو يرفضه من التطبيق أو من مسار `/access-request/approve`، ويمكن لأي من الجانبين استقصاء الحالة الراهنة

### تعديلات الشرائح

- `add-slide` — إلحاق شريحة واحدة (أو إدراجها في موضع معين). استدعِه بالتتابع بعد `create-deck` فارغ حتى يستطيع المحرر إظهار التقدّم.
- `update-slide` — تعديل دقيق بالبحث والاستبدال أو تعديل كامل لمحتوى الشريحة
- `patch-deck` — قائمة انتظار العمليات الدقيقة التي يستخدمها محرر المتصفح بدلاً من عملية PUT لكل المجموعة
- `get-layout-overflows` — قراءة نتائج مقاسة في المتصفح لمدى ملاءمة الشريحة لتخطيطها، حتى يمكن اكتشاف تعديل لم يعد يناسب تخطيطه وتنقيحه

### السجل

- `list-deck-versions`، `get-deck-version`، `restore-deck-version` — تأخذ كل عملية كتابة لقطة أولًا، لذا يمكن سرد أي لحظة زمنية واستعادتها

### التعليقات

- `list-slide-comments`، `add-slide-comment`، `update-slide-comment`، `delete-slide-comment`

### الاستيراد

- `import-pptx`، `import-docx`، `import-google-doc` — مستوردات مخصصة لتنسيق واحد
- `import-google-slides-reference` — استيراد مجموعة Google Slides كمجموعة مرجعية قابلة لإعادة الاستخدام، بدلاً من سحب محتواها إلى المجموعة العاملة
- `import-file` — نقطة دخول واحدة لـ PPTX أو DOCX أو PDF، أو ملف Figma بصيغة `.fig` (والذي يوجَّه إلى فهرسة نظام تصميم Builder بدلاً من استيراد شرائح)
- `extract-pdf` — تفريغ نص خام من PDF، متاح فقط عبر CLI، ويُستخدم غالبًا للتصحيح المحلي

### التصدير

- `export-pptx` — ملف PowerPoint أصلي قابل للتحرير
- `export-google-slides` — إنشاء نفس ملف PPTX بالإضافة إلى رابط استيراد جاهز إلى Google Slides
- `export-html` — ملف HTML مستقل وذاتي الاحتواء بتنقّل مدمج بلوحة المفاتيح

### أنظمة التصميم

- `create-design-system`، `update-design-system`، `delete-design-system`، `get-design-system`، `list-design-systems`، `set-default-design-system`، `apply-design-system` (ربط نظام بمجموعة)
- `get-workspace-defaults`، `set-workspace-defaults` — المجموعة ونظام التصميم الافتراضيان على مستوى مساحة العمل بأكملها. تعيين أي منهما مقصور على المسؤول
- `analyze-brand-assets`، `import-document`، `import-from-url`، `import-code` — جمع إشارات علامة تجارية خام (CSS/خطوط الموقع، أو محتوى مستند أو ملف كود مرفوع) لنظام تصميم **محلي** تصوغه الوكيل
- `index-design-system-with-builder`، `sync-design-system-with-builder`، `import-design-project` — المسار المفهرس عبر Builder لملفات Figma، وقواعد الكود/مستودعات GitHub المتصلة، وملف `design.md`. يبدأ `import-design-project` من نظام تصميم موجود بدلاً من مصدر خام

### السياق الإبداعي

- `clone-context-slide` — إدراج شريحة واحدة مثبَّتة من المكتبة في مجموعة دون تغيير، بلا إعادة إنشاء HTML/CSS الخاصة بها
- `clone-creative-context-deck` — إعادة إنشاء نسخة محددة بدقة من مجموعة محكومة
- `get-deck-reference-context` — استخراج اللغة البصرية لمجموعة موجودة كمكتبة أنماط قابلة لإعادة الاستخدام
- الاسترجاع وتقديم المحتوى إلى المكتبة (البحث، وجلب عنصر سياق، وإدارة عضوية المكتبة) إجراءات مشتركة من إطار العمل يستدعيها الوكيل بالطريقة نفسها في كل تطبيق يدعم السياق الإبداعي. راجع مهارة `creative-context` لمعرفة إجراء الاسترجاع وسلّم إعادة الاستخدام.

### التصعيد والبيانات المُدرجة

- `provider-api-catalog`، `provider-api-docs`، `provider-api-request` — وصول خام إلى واجهة برمجة تطبيقات Google Drive عندما يكون إجراء استيراد/تصدير جاهز ضيقًا جدًا. تحلّ الشرائح المصادقة من حساب Google Docs المتصل بالمستخدم
- `list-staged-datasets`، `query-staged-dataset`، `delete-staged-dataset` — تجميع نتيجة مزوّد كبيرة مُدرجة دون إعادة جلبها

### التنقل والسياق

- `view-screen` — ما ينظر إليه المستخدم حاليًا: المجموعة المفتوحة، والشريحة الحالية، وأي تحديد بصري نشط
- `navigate` — نقل واجهة المستخدم إلى مجموعة، أو شريحة، أو عرض

## مرجع المسارات

| المسار                           | يعرض                                                                       |
| -------------------------------- | -------------------------------------------------------------------------- |
| `/`                              | صفحة تسويقية/SEO عامة                                                      |
| `/home`                          | قائمة المجموعات، الصفحة الرئيسية الخاصة للتطبيق                            |
| `/deck/:id`                      | المحرر                                                                     |
| `/deck/:id/present`              | العرض التقديمي بملء الشاشة                                                 |
| `/p/:id`                         | معاينة مباشرة للمجموعة عامة/قابلة لقراءة الوكيل، مختلفة عن `/share/:token` |
| `/share/:token`                  | لقطة عامة للقراءة فقط، مجمّدة عند وقت المشاركة                             |
| `/slide`                         | تضمين شريحة واحدة يُستخدم في معاينات الدردشة                               |
| `/design-systems`                | إدارة أنظمة التصميم المحفوظة                                               |
| `/access-request/approve`        | الموافقة على طلب وصول إلى مجموعة خاصة                                      |
| `/agent`، `/extensions`، `/team` | إعادة توجيه إلى تبويب `/settings` المناسب                                  |

## تخصيصه

قالب الشرائح قابل للتخصيص بالكامل. الأماكن الرئيسية التي يجب البحث فيها عند توسيعه:

#### الإجراءات — `templates/slides/actions/`

تعيش هنا كل عملية يمكن للوكيل استدعاءها كملف TypeScript، ويتم تركيبها تلقائيًا
على `POST /_agent-native/actions/:name` ويمكن استدعاؤها من CLI كـ
`pnpm action <name>`. أضف ملفًا جديدًا هنا لمنح الوكيل قدرة جديدة.

<AnnotatedCode
  id="doc-block-slides9"
  title="تشريح إجراء تعديل شريحة"
  filename="actions/update-slide.ts (مُبسّط)"
  language="ts"
  code={
    'import { defineAction, fail } from "@agent-native/core/action";\nimport { assertAccess } from "@agent-native/core/sharing";\nimport { eq } from "drizzle-orm";\nimport { z } from "zod";\n\nimport { getDb, schema } from "../server/db/index.js";\nimport { notifyClients } from "../server/handlers/decks.js";\nimport { withDeckLock } from "./patch-deck.js";\n\nexport default defineAction({\n  description:\n    "Surgically edit a slide\'s content using search-replace or full replacement. " +\n    "Syncs live to open editors. Prefer this over full deck rewrites.",\n  schema: z.object({\n    deckId: z.string(),\n    slideId: z.string(),\n    find: z.string().optional(),\n    replace: z.string().optional(),\n    fullContent: z.string().optional(),\n  }),\n  run: async ({ deckId, slideId, find, replace, fullContent }) => {\n    await assertAccess("deck", deckId, "editor");\n    return withDeckLock(deckId, async () => {\n      const db = getDb();\n      const [row] = await db\n        .select()\n        .from(schema.decks)\n        .where(eq(schema.decks.id, deckId));\n      if (!row) {\n        fail(`Deck ${deckId} not found`, { errorCode: "deck_not_found", statusCode: 404 });\n      }\n\n      const deck = JSON.parse(row.data);\n      const slide = deck.slides.find((s: { id: string }) => s.id === slideId);\n      if (!slide) {\n        fail(`Slide ${slideId} not found`, { errorCode: "slide_not_found", statusCode: 404 });\n      }\n\n      if (fullContent) {\n        slide.content = fullContent;\n      } else if (find) {\n        const idx = slide.content.indexOf(find);\n        if (idx === -1) {\n          fail("Text not found", { errorCode: "slide_find_no_match" });\n        }\n        slide.content =\n          slide.content.slice(0, idx) +\n          (replace ?? "") +\n          slide.content.slice(idx + find.length);\n      }\n\n      deck.updatedAt = new Date().toISOString();\n      await db\n        .update(schema.decks)\n        .set({ data: JSON.stringify(deck), updatedAt: deck.updatedAt })\n        .where(eq(schema.decks.id, deckId));\n\n      notifyClients(deckId, { slideId, actor: "agent" });\n      return { ok: true, deckId, slideId };\n    });\n  },\n});'
  }
  annotations={[
    {
      lines: "22",
      label: "فحص الوصول",
      note: "يُطلق `assertAccess` خطأ قبل أن يعمل أي شيء آخر إذا لم يكن المستدعي محررًا على الأقل لهذه المجموعة.",
    },
    {
      lines: "23",
      label: "قفل خاص بكل مجموعة",
      note: "يُسلسِل `withDeckLock` هذه العملية مقابل كل كتابة أخرى إلى نفس المجموعة. تتشارك `add-slide` و`patch-deck` الخاصة بالمتصفح في نفس القفل، لذا لا يتلف كاتبان يعدّلان شرائح مختلفة عمل أحدهما الآخر أبدًا.",
    },
    {
      lines: "29-37",
      label: "الفشل بصوت عالٍ، لا الإرجاع بصمت",
      note: 'تُطلق `fail(...)` خطأ منظّمًا بدلاً من إرجاع قيمة عادية. كان هذا الإجراء يُرجع سابقًا `{ ok: false, message: "Text not found" }` عند عدم وجود تطابق، وهو ما يمكن أن يقرأه المستدعي بسهولة زائدة كنجاح. إطلاق الخطأ هو القناة الوحيدة التي تضمن الإخبار بأن المجموعة لم تُعدَّل.',
    },
    {
      lines: "39-51",
      label: "تعديل دقيق",
      note: "يغيّر زوج `find`/`replace` النص المطابق فقط، ويفشل الآن بصوت عالٍ أيضًا عند عدم وجود تطابق. يستبدل `fullContent` الشريحة كاملة. فضّل الصيغة الأضيق للتعديلات الحقيقية.",
    },
    {
      lines: "53-57",
      label: "SQL هو المصدر الوحيد للحقيقة",
      note: "المجموعة كاملة هي كائن JSON واحد في `decks.data`. لا يوجد جدول `slides` منفصل، لذا تقرأ كل كتابة ذلك العمود الواحد وتعدّله وتعيد كتابته.",
    },
    {
      lines: "59",
      label: "المزامنة المباشرة",
      note: "تدفع `notifyClients` التغيير إلى أي محرر مفتوح فورًا. الاستقصاء (polling) هو المسار البديل. راجع [التعاون في الوقت الفعلي](/docs/real-time-collaboration).",
    },
  ]}
/>

يتحقق الإجراء الحقيقي أيضًا من تقديم نمط تعديل واحد بالضبط، ويأخذ لقطة إصدار
قبل الكتابة، ويكتب مع فحص تزامن متفائل حتى لا تستطيع قراءة قديمة الكتابة فوق
قراءة أحدث بصمت أبدًا، فضلًا عن تسجيل مصدر إعادة استخدام السياق الإبداعي
والانتظار لحظة قصيرة حتى يُبلّغ المحرر عن قياس فائض في التخطيط. يُبسّط هذا
المثال كل ذلك إلى الشكل الجوهري: الوصول، والقفل، والقراءة-التعديل-الكتابة،
والإشعار.

#### التخطيط

<FileTree
  id="doc-block-slides8"
  title="بنية templates/slides/"
  entries={[
    {
      path: "actions/create-deck.ts",
      note: "مجموعة جديدة، مع تعبئة مسبقة اختيارية بالشرائح، أو استبدال مجمّع وذري",
    },
    {
      path: "actions/add-slide.ts",
      note: "إلحاق شريحة واحدة، يُستخدم للإنشاء المتدفق",
    },
    {
      path: "actions/update-slide.ts",
      note: "تعديل بالبحث/الاستبدال أو تعديل كامل لمحتوى الشريحة",
    },
    {
      path: "actions/patch-deck.ts",
      note: "عمليات دقيقة يستخدمها محرر المتصفح بدلاً من عملية PUT لكل المجموعة",
    },
    {
      path: "actions/index-design-system-with-builder.ts",
      note: "يبدأ فهرسة نظام تصميم Builder من Figma أو GitHub أو الكود",
    },
    {
      path: "app/routes/deck.$id.tsx",
      note: "محرر المجموعة",
    },
    {
      path: "app/routes/deck.$id_.present.tsx",
      note: "وضع العرض التقديمي بملء الشاشة",
    },
    {
      path: "app/routes/agent.tsx",
      note: "تبويب الوكيل بملء الصفحة",
    },
    {
      path: "app/routes/p.$id.tsx",
      note: "معاينة مجموعة واحدة عامة/قابلة لقراءة الوكيل",
    },
    {
      path: "app/routes/share.$token.tsx",
      note: "صفحة لقطة عامة للقراءة فقط",
    },
    {
      path: "app/components/editor/",
      note: "لوحة الشريحة، وشريط الأدوات، والشريط الجانبي، واللوحات",
    },
    {
      path: "server/db/schema.ts",
      note: "decks, slide_comments, deck_versions, design_systems, deck_share_links",
    },
    {
      path: ".agents/skills/create-deck/SKILL.md",
      note: "قوالب HTML دقيقة للشرائح لكل تخطيط",
    },
    {
      path: ".agents/skills/creative-context/SKILL.md",
      note: "سلّم إعادة الاستخدام وإجراء الاسترجاع",
    },
  ]}
/>

يحدث معظم تخصيص واجهة المحرر تحت `app/components/editor/`: لوحة الشريحة،
وشريط الأدوات، والشريط الجانبي، وقائمة الفقاعة، وقائمة الشرطة المائلة، ولوحة
السجل.

#### المهارات (Skills) — `templates/slides/.agents/skills/`

مهارات الوكيل التي تشرح الأنماط عندما يحتاج الوكيل إلى تعديل الكود:

- `create-deck` — كيفية إنشاء مجموعة جديدة، مع قوالب HTML دقيقة للشرائح لكل تخطيط
- `slide-editing` — كيفية تحرير الشرائح الفردية
- `deck-management` — كيفية تخزين المجموعات والوصول إليها
- `design-systems` — شكل بيانات نظام التصميم وكيفية تطبيق الرموز المميزة على الشرائح المُنشأة
- `creative-context` — سلّم إعادة الاستخدام، وإجراء الاسترجاع، وتقديم المحتوى إلى المكتبة

#### AGENTS.md

`templates/slides/AGENTS.md` هو جهاز التوجيه القصير الذي يقرأه الوكيل في كل
محادثة. يشير إلى المهارات ضمن `.agents/skills/` ويضع القواعد الأساسية، ونموذج
الاستمرارية، وعقد حالة التطبيق. حدّث المهارة المعنية كل مرة تضيف أو تغيّر
فيها تخطيط شريحة أو مصدر نظام تصميم.

## ما التالي

- [**الميزات**](/docs/template-slides-features) — مسار التحرير، والتقديم، والمشاركة، ونظام التصميم، والاستيراد والتصدير الذي يظهر للمستخدم والذي يدعمه نموذج البيانات هذا
- [**التحدث مع الوكيل**](/docs/template-slides-agent) — المطالبات بحسب المهمة وما يراه الوكيل
- [**الشرائح في مساحة عمل متعددة التطبيقات**](/docs/template-slides-integrations) — كيف يتم استدعاء هذه الإجراءات من خارج الشرائح
- [**المشاركة**](/docs/sharing) — نموذج منح المشاركة الذي تُبنى عليه `deck_shares` و`design_system_shares`
- [**التعاون في الوقت الفعلي**](/docs/real-time-collaboration) — خط أنابيب Yjs الذي تشترك فيه المجموعات مع أسطح تعاونية أخرى
- [**نظرة عامة على الشرائح**](/docs/template-slides) — رجوع إلى ملخص التطبيق
