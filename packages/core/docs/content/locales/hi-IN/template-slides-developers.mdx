---
title: "स्लाइड: डेवलपर गाइड"
description: "स्लाइड का डेटा मॉडल, पर्सिस्टेंस मॉडल, पूरी action इन्वेंटरी, रूट्स, और टेम्पलेट को कस्टमाइज़ या एक्सटेंड करते समय सामान्य बदलाव कहां करें।"
---

# डेवलपर गाइड

यह पेज उन सभी के लिए है जो स्लाइड टेम्पलेट को कस्टमाइज़ या एक्सटेंड कर रहे हैं: SQL डेटा मॉडल, कॉनकर्रेंट राइटर को कैसे सुरक्षित रखा जाता है, हर एजेंट-कॉल करने योग्य action, रूट मैप, और सामान्य बदलाव कहां करें।

## क्विक स्टार्ट

```bash
npx @agent-native/core@latest create my-slides --standalone --template slides
```

बाकी सेटअप (`cd`, `pnpm install`, `pnpm dev`) और फ़्रेमवर्क की बुनियादी बातों के लिए [शुरुआत करें](/docs/getting-started) देखें।

## पर्सिस्टेंस मॉडल

हर डेक `decks.data` में एक JSON ब्लॉब है। हर राइट एक सर्वर-साइड रीड-मॉडिफ़ाई-राइट action से गुज़रता है जो एक पर-डेक लॉक होल्ड करता है, ताकि कॉनकर्रेंट राइटर, कोई इंसान और एजेंट, या दो इंसान, एक ही डेक की अलग-अलग स्लाइड को छूते हुए भी कभी एक-दूसरे का काम ओवरराइट न करें।

- **एजेंट actions** (`add-slide`, `update-slide`) अपने खुद के डेडिकेटेड, ग्रैन्युलर actions इस्तेमाल करते हैं और वही लॉक शेयर करते हैं।
- **ब्राउज़र एडिटर** पूरे डेक के PUT के बजाय `patch-deck` के ज़रिए ग्रैन्युलर ऑप्स (`patch-slide`, `delete-slide`, `reorder-slides`, `add-slide`, `patch-deck-fields`) एनक्यू करता है।

<Callout id="doc-block-slides10" tone="warning">

अगर आप एडिटर के सेव पाथ को एक्सटेंड कर रहे हैं, तो `patch-deck` के ज़रिए एक नया ऑप एनक्यू करें। कोई नया पूरे डेक का PUT न जोड़ें। इससे वह लास्ट-राइट-विंस रेस दोबारा आ जाएगी जिसे रोकने के लिए यह लॉक मौजूद है।

</Callout>

## डेटा मॉडल

सारा डेक डेटा Drizzle ORM के ज़रिए SQL में रहता है। स्कीमा: `templates/slides/server/db/schema.ts`.

<DataModel
  id="doc-block-slides7"
  title="स्लाइड डेटा मॉडल"
  summary={
    "एक डेक अपनी स्लाइड्स को decks.data में JSON के तौर पर रखता है। कमेंट, वर्शन, शेयर, और डिज़ाइन सिस्टम इसी से जुड़े होते हैं।"
  }
  entities={[
    {
      id: "decks",
      name: "decks",
      note: "स्लाइड्स data में JSON के तौर पर रहती हैं, ownableColumns के साथ",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
          note: "e.g. deck-1712345-abc",
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "data",
          type: "text",
          note: "JSON: { title, slides: [{ id, content, layout }] }",
        },
        {
          name: "design_system_id",
          type: "text",
          nullable: true,
        },
        {
          name: "created_at",
          type: "text",
        },
        {
          name: "updated_at",
          type: "text",
        },
      ],
    },
    {
      id: "slide_comments",
      name: "slide_comments",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
        },
        {
          name: "deck_id",
          type: "text",
          fk: "decks.id",
        },
        {
          name: "slide_id",
          type: "text",
          note: "वह स्लाइड जिस पर कमेंट है",
        },
        {
          name: "thread_id",
          type: "text",
          note: "थ्रेडिंग",
        },
        {
          name: "parent_id",
          type: "text",
          nullable: true,
        },
        {
          name: "content",
          type: "text",
        },
        {
          name: "quoted_text",
          type: "text",
          nullable: true,
        },
        {
          name: "author_email",
          type: "text",
        },
        {
          name: "author_name",
          type: "text",
          nullable: true,
        },
        {
          name: "resolved",
          type: "boolean",
        },
      ],
    },
    {
      id: "deck_versions",
      name: "deck_versions",
      note: "रीस्टोर के लिए पॉइंट-इन-टाइम स्नैपशॉट",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
        },
        {
          name: "deck_id",
          type: "text",
          fk: "decks.id",
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "data",
          type: "text",
          note: "पूरा डेक JSON",
        },
        {
          name: "change_label",
          type: "text",
          nullable: true,
        },
      ],
    },
    {
      id: "design_systems",
      name: "design_systems",
      note: "दोबारा इस्तेमाल किए जा सकने वाले ब्रांड टोकन, ownableColumns के साथ",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "description",
          type: "text",
          nullable: true,
        },
        {
          name: "data",
          type: "text",
          note: "colors / typography / spacing",
        },
        {
          name: "assets",
          type: "text",
          nullable: true,
        },
        {
          name: "custom_instructions",
          type: "text",
        },
        {
          name: "is_default",
          type: "boolean",
        },
      ],
    },
    {
      id: "deck_share_links",
      name: "deck_share_links",
      note: "पर्सिस्टेड पब्लिक शेयर-लिंक स्नैपशॉट",
      fields: [
        {
          name: "token",
          type: "text",
          pk: true,
        },
        {
          name: "title",
          type: "text",
        },
        {
          name: "slides",
          type: "text",
          note: "JSON स्लाइड्स स्नैपशॉट",
        },
        {
          name: "aspect_ratio",
          type: "text",
          nullable: true,
        },
        {
          name: "created_at",
          type: "text",
        },
      ],
    },
  ]}
  relations={[
    {
      from: "decks",
      to: "slide_comments",
      kind: "1-n",
      label: "कमेंट",
    },
    {
      from: "decks",
      to: "deck_versions",
      kind: "1-n",
      label: "स्नैपशॉट",
    },
    {
      from: "design_systems",
      to: "decks",
      kind: "1-n",
      label: "पर लागू",
    },
  ]}
/>

फ़्रेमवर्क की शेयर टेबल (`deck_shares`, `design_system_shares`) प्रिंसिपल्स, यूज़र या ऑर्ग, को हर रिसोर्स के लिए viewer / editor / admin भूमिकाओं पर मैप करती हैं, और हाथ से लिखने के बजाय शेयर्ड `createSharesTable` हेल्पर के ज़रिए बनाई जाती हैं।

#### स्लाइड की संरचना

`decks.data` के अंदर हर स्लाइड यह होती है:

```json
{
  "id": "slide-1",
  "layout": "title",
  "content": "<div class=\"fmd-slide\" style=\"...\">...</div>"
}
```

`content` कच्चा HTML है। रेंडरर फ़िक्स्ड एस्पेक्ट रेशियो और बैकग्राउंड देता है, और HTML अंदर की हर चीज़ देता है। रिच एम्बेडिंग भी सपोर्टेड है: हाथ से बने डायग्राम और लाइव-रेंडर्ड चार्ट प्लेन HTML के साथ किसी स्लाइड के अंदर रह सकते हैं।

## Actions इन्वेंटरी

### डेक लाइफ़साइकल

- `create-deck` — नया डेक, ज़रूरत पड़े तो स्लाइड्स से पहले से भरा हुआ, या किसी मौजूदा डेक की स्लाइड्स का एटॉमिक बल्क रिप्लेस। खाली डेक के लिए `slides` छोड़ दें या `[]` पास करें। यह action application state के ज़रिए नए डेक को खोल देता है।
- `get-deck` — पूरा डेक JSON, या एक कॉम्पैक्ट स्लाइड-समरी व्यू
- `list-decks` — कॉलर जो भी डेक देख सकता है, सस्ती पोलिंग के लिए light/compact/full-slide प्रोजेक्शन के साथ
- `duplicate-deck` — नई स्लाइड ids के साथ किसी डेक की कॉपी
- `update-deck-aspect-ratio` — 16:9, 1:1, 9:16, या 4:5

`add-deck`, `save-deck`, और `delete-deck` ब्राउज़र एडिटर के ऑप्टिमिस्टिक-क्रिएट, अनडू/रीडू, और डिलीट फ़्लो को सपोर्ट करते हैं। एजेंट इसके बदले `create-deck`, `add-slide`/`update-slide`/`patch-deck`, और फ़्रेमवर्क के अपने शेयरिंग actions इस्तेमाल करता है।

### एक्सेस रिक्वेस्ट

- `request-deck-access`, `approve-deck-access-request`, `get-deck-access-status` — प्राइवेट-डेक एक्सेस-रिक्वेस्ट फ़्लो: बिना एक्सेस वाला कोई विज़िटर इसे रिक्वेस्ट करता है, ओनर ऐप से या `/access-request/approve` रूट से अप्रूव या डिनाई करता है, और दोनों तरफ़ से मौजूदा स्टेटस पोल किया जा सकता है

### स्लाइड एडिट

- `add-slide` — एक स्लाइड अपेंड करें (या किसी पोज़िशन पर इंसर्ट करें)। इसे किसी खाली `create-deck` के बाद सीक्वेंशियली कॉल करें ताकि एडिटर प्रोग्रेस दिखा सके।
- `update-slide` — सर्जिकल फ़ाइंड/रिप्लेस या फ़ुल-कॉन्टेंट स्लाइड एडिट
- `patch-deck` — वह ग्रैन्युलर ऑप क्यू जिसे ब्राउज़र एडिटर पूरे डेक के PUT के बजाय इस्तेमाल करता है
- `get-layout-overflows` — ब्राउज़र से मेज़र्ड स्लाइड-फ़िट नतीजे पढ़ें, ताकि कोई एडिट जो अपने लेआउट में फ़िट नहीं होता, पकड़ा और ठीक किया जा सके

### हिस्ट्री

- `list-deck-versions`, `get-deck-version`, `restore-deck-version` — हर राइट पहले स्नैपशॉट लेता है, ताकि किसी भी पॉइंट-इन-टाइम को लिस्ट और रीस्टोर किया जा सके

### कमेंट

- `list-slide-comments`, `add-slide-comment`, `update-slide-comment`, `delete-slide-comment`

### इम्पोर्ट

- `import-pptx`, `import-docx`, `import-google-doc` — डेडिकेटेड सिंगल-फ़ॉर्मेट इम्पोर्टर
- `import-google-slides-reference` — किसी Google Slides डेक को उसके कॉन्टेंट को वर्किंग डेक में खींचने के बजाय एक दोबारा इस्तेमाल किए जा सकने वाले रेफ़रेंस डेक के तौर पर इम्पोर्ट करें
- `import-file` — PPTX, DOCX, या PDF के लिए, या किसी Figma `.fig` फ़ाइल के लिए एक एंट्री पॉइंट (जो स्लाइड इम्पोर्ट के बजाय Builder डिज़ाइन-सिस्टम इंडेक्सिंग में जाती है)
- `extract-pdf` — किसी PDF का CLI-only कच्चा टेक्स्ट डंप, ज़्यादातर लोकल डीबगिंग के लिए

### एक्सपोर्ट

- `export-pptx` — नेटिव, एडिटेबल PowerPoint
- `export-google-slides` — वही PPTX जेनरेट करता है, साथ में एक तैयार Google Slides इम्पोर्ट लिंक
- `export-html` — बिल्ट-इन कीबोर्ड नेविगेशन के साथ एक स्टैंडअलोन, सेल्फ़-कंटेन्ड HTML फ़ाइल

### डिज़ाइन सिस्टम

- `create-design-system`, `update-design-system`, `delete-design-system`, `get-design-system`, `list-design-systems`, `set-default-design-system`, `apply-design-system` (किसी एक को डेक से लिंक करें)
- `get-workspace-defaults`, `set-workspace-defaults` — वर्कस्पेस-वाइड डिफ़ॉल्ट डेक और डिज़ाइन सिस्टम। सेट करना सिर्फ़ एडमिन के लिए है
- `analyze-brand-assets`, `import-document`, `import-from-url`, `import-code` — एक **लोकल**, एजेंट-ड्राफ़्टेड डिज़ाइन सिस्टम के लिए कच्चे ब्रांड सिग्नल इकट्ठा करें (वेबसाइट CSS/फ़ॉन्ट, अपलोडेड डॉक्यूमेंट या कोड फ़ाइल कॉन्टेंट)
- `index-design-system-with-builder`, `sync-design-system-with-builder`, `import-design-project` — Figma फ़ाइलों, कनेक्टेड कोड/GitHub रेपो, और `design.md` के लिए Builder-इंडेक्स्ड पाथ। `import-design-project` किसी कच्चे सोर्स के बजाय किसी मौजूदा डिज़ाइन सिस्टम से शुरू होता है

### Creative Context

- `clone-context-slide` — किसी पिन की हुई Library स्लाइड को बिना बदले, उसके HTML/CSS को फिर से जेनरेट किए बिना, किसी डेक में इंसर्ट करें
- `clone-creative-context-deck` — किसी एग्ज़ैक्ट गवर्न्ड डेक वर्शन को दोबारा बनाएं
- `get-deck-reference-context` — किसी मौजूदा डेक की विज़ुअल भाषा को एक दोबारा इस्तेमाल किए जा सकने वाली पैटर्न लाइब्रेरी के तौर पर एक्सट्रैक्ट करें
- रिट्रीवल और Library सबमिशन (सर्च करना, कोई कॉन्टेक्स्ट आइटम फ़ेच करना, Library मेंबरशिप मैनेज करना) शेयर्ड फ़्रेमवर्क actions हैं जिन्हें एजेंट Creative Context सपोर्ट करने वाले हर ऐप में एक जैसे तरीके से कॉल करता है। रिट्रीवल प्रोसीज़र और रीयूज़ लैडर के लिए `creative-context` skill देखें।

### एस्केलेशन और स्टेज्ड डेटा

- `provider-api-catalog`, `provider-api-docs`, `provider-api-request` — जब कोई बना-बनाया इम्पोर्ट/एक्सपोर्ट action बहुत संकरा हो, तो रॉ Google Drive API एक्सेस। स्लाइड यूज़र के कनेक्टेड Google Docs अकाउंट से ऑथ रिज़ॉल्व करता है
- `list-staged-datasets`, `query-staged-dataset`, `delete-staged-dataset` — किसी बड़े स्टेज्ड प्रोवाइडर नतीजे को दोबारा फ़ेच किए बिना एग्रीगेट करें

### नेविगेशन और कॉन्टेक्स्ट

- `view-screen` — यूज़र इस वक़्त क्या देख रहा है: खुला डेक, मौजूदा स्लाइड, और कोई भी एक्टिव विज़ुअल सिलेक्शन
- `navigate` — UI को किसी डेक, स्लाइड, या व्यू पर मूव करें

## रूट्स रेफ़रेंस

| रूट                              | रेंडर करता है                                                 |
| -------------------------------- | ------------------------------------------------------------- |
| `/`                              | पब्लिक मार्केटिंग/SEO लैंडिंग पेज                             |
| `/home`                          | डेक लिस्ट, प्राइवेट ऐप होम                                    |
| `/deck/:id`                      | एडिटर                                                         |
| `/deck/:id/present`              | फ़ुल-स्क्रीन प्रेजेंटेशन                                      |
| `/p/:id`                         | पब्लिक/एजेंट-रीडेबल लाइव डेक प्रीव्यू, `/share/:token` से अलग |
| `/share/:token`                  | पब्लिक रीड-ओनली स्नैपशॉट, शेयर के वक़्त फ़्रीज़ किया गया      |
| `/slide`                         | चैट प्रीव्यू के लिए इस्तेमाल होने वाला सिंगल-स्लाइड एम्बेड    |
| `/design-systems`                | सेव किए हुए डिज़ाइन सिस्टम मैनेज करें                         |
| `/access-request/approve`        | किसी प्राइवेट डेक के एक्सेस रिक्वेस्ट को अप्रूव करें          |
| `/agent`, `/extensions`, `/team` | संबंधित `/settings` टैब पर रीडायरेक्ट करें                    |

## इसे कस्टमाइज़ करना

स्लाइड टेम्पलेट पूरी तरह से कस्टमाइज़ेबल है। इसे एक्सटेंड करते समय देखने लायक मुख्य जगहें:

#### Actions — `templates/slides/actions/`

हर एजेंट-कॉल करने योग्य ऑपरेशन यहां एक TypeScript फ़ाइल के तौर पर रहता है, जो अपने आप `POST /_agent-native/actions/:name` पर माउंट हो जाती है और CLI से `pnpm action <name>` के तौर पर कॉल की जा सकती है। एजेंट को कोई नई क्षमता देने के लिए यहां एक नई फ़ाइल जोड़ें।

<AnnotatedCode
  id="doc-block-slides9"
  title="स्लाइड एडिट action की एनाटॉमी"
  filename="actions/update-slide.ts (simplified)"
  language="ts"
  code={
    'import { defineAction, fail } from "@agent-native/core/action";\nimport { assertAccess } from "@agent-native/core/sharing";\nimport { eq } from "drizzle-orm";\nimport { z } from "zod";\n\nimport { getDb, schema } from "../server/db/index.js";\nimport { notifyClients } from "../server/handlers/decks.js";\nimport { withDeckLock } from "./patch-deck.js";\n\nexport default defineAction({\n  description:\n    "Surgically edit a slide\'s content using search-replace or full replacement. " +\n    "Syncs live to open editors. Prefer this over full deck rewrites.",\n  schema: z.object({\n    deckId: z.string(),\n    slideId: z.string(),\n    find: z.string().optional(),\n    replace: z.string().optional(),\n    fullContent: z.string().optional(),\n  }),\n  run: async ({ deckId, slideId, find, replace, fullContent }) => {\n    await assertAccess("deck", deckId, "editor");\n    return withDeckLock(deckId, async () => {\n      const db = getDb();\n      const [row] = await db\n        .select()\n        .from(schema.decks)\n        .where(eq(schema.decks.id, deckId));\n      if (!row) {\n        fail(`Deck ${deckId} not found`, { errorCode: "deck_not_found", statusCode: 404 });\n      }\n\n      const deck = JSON.parse(row.data);\n      const slide = deck.slides.find((s: { id: string }) => s.id === slideId);\n      if (!slide) {\n        fail(`Slide ${slideId} not found`, { errorCode: "slide_not_found", statusCode: 404 });\n      }\n\n      if (fullContent) {\n        slide.content = fullContent;\n      } else if (find) {\n        const idx = slide.content.indexOf(find);\n        if (idx === -1) {\n          fail("Text not found", { errorCode: "slide_find_no_match" });\n        }\n        slide.content =\n          slide.content.slice(0, idx) +\n          (replace ?? "") +\n          slide.content.slice(idx + find.length);\n      }\n\n      deck.updatedAt = new Date().toISOString();\n      await db\n        .update(schema.decks)\n        .set({ data: JSON.stringify(deck), updatedAt: deck.updatedAt })\n        .where(eq(schema.decks.id, deckId));\n\n      notifyClients(deckId, { slideId, actor: "agent" });\n      return { ok: true, deckId, slideId };\n    });\n  },\n});'
  }
  annotations={[
    {
      lines: "22",
      label: "एक्सेस चेक",
      note: "`assertAccess` कुछ भी और चलने से पहले थ्रो करता है अगर कॉलर इस डेक पर कम से कम editor भी नहीं है।",
    },
    {
      lines: "23",
      label: "पर-डेक लॉक",
      note: "`withDeckLock` इसे उस डेक पर होने वाले हर दूसरे राइट के साथ सीरियलाइज़ करता है। `add-slide` और ब्राउज़र का `patch-deck` वही लॉक शेयर करते हैं, इसलिए अलग-अलग स्लाइड को छूने वाले दो राइटर कभी एक-दूसरे का काम खराब नहीं करते।",
    },
    {
      lines: "29-37",
      label: "ज़ोर से fail हो, चुपचाप रिटर्न मत करो",
      note: '`fail(...)` किसी सादे वैल्यू को रिटर्न करने के बजाय एक स्ट्रक्चर्ड एरर थ्रो करता है। यह action पहले किसी खराब मैच के लिए `{ ok: false, message: "Text not found" }` रिटर्न करता था, जिसे कोई कॉलर बहुत आसानी से सक्सेस समझ सकता था। थ्रो करना ही वह इकलौता चैनल है जो भरोसे के साथ बताता है कि डेक मॉडिफ़ाई नहीं हुआ।',
    },
    {
      lines: "39-51",
      label: "सर्जिकल एडिट",
      note: "कोई `find`/`replace` पेयर सिर्फ़ मैच हुए टेक्स्ट को बदलता है, और अब कोई मैच न मिलने पर भी ज़ोर से fail होता है। `fullContent` पूरी स्लाइड स्वैप कर देता है। असली एडिट के लिए संकरे फ़ॉर्म को प्रेफ़र करें।",
    },
    {
      lines: "53-57",
      label: "SQL ही सोर्स ऑफ़ ट्रुथ है",
      note: "पूरा डेक `decks.data` में एक JSON ब्लॉब है। कोई अलग `slides` टेबल नहीं है, इसलिए हर राइट उस एक कॉलम को पढ़ता, एडिट करता, और फिर से लिखता है।",
    },
    {
      lines: "59",
      label: "लाइव सिंक",
      note: "`notifyClients` बदलाव को किसी भी खुले एडिटर पर तुरंत पुश करता है। पोलिंग फ़ॉलबैक पाथ है। देखें [रीयल-टाइम कोलैबोरेशन](/docs/real-time-collaboration)।",
    },
  ]}
/>

असली action यह भी वैलिडेट करता है कि सिर्फ़ एक ही एडिट मोड दिया गया था, राइट करने से पहले एक वर्शन का स्नैपशॉट लेता है, और एक ऑप्टिमिस्टिक-कॉनकर्रेंसी चेक के साथ लिखता है ताकि कोई स्टेल रीड कभी किसी नए को चुपचाप ओवरराइट न कर सके, साथ ही Creative Context रीयूज़ प्रोवेनन्स रिकॉर्ड करता है और एडिटर के लेआउट-ओवरफ़्लो मेज़रमेंट रिपोर्ट करने का थोड़ा इंतज़ार करता है। यह उदाहरण उस सबको कोर एक्सेस, लॉक, रीड-मॉडिफ़ाई-राइट, नोटिफ़ाई शेप तक सीमित कर देता है।

#### लेआउट

<FileTree
  id="doc-block-slides8"
  title="templates/slides/ की संरचना"
  entries={[
    {
      path: "actions/create-deck.ts",
      note: "नया डेक, ज़रूरत पड़े तो स्लाइड्स से पहले से भरा हुआ, या एक एटॉमिक बल्क रिप्लेस",
    },
    {
      path: "actions/add-slide.ts",
      note: "एक स्लाइड अपेंड करें, स्ट्रीमिंग जेनरेशन के लिए इस्तेमाल होता है",
    },
    {
      path: "actions/update-slide.ts",
      note: "फ़ाइंड/रिप्लेस या फ़ुल-कॉन्टेंट स्लाइड एडिट",
    },
    {
      path: "actions/patch-deck.ts",
      note: "ग्रैन्युलर ऑप्स जिन्हें ब्राउज़र एडिटर पूरे डेक के PUT के बजाय इस्तेमाल करता है",
    },
    {
      path: "actions/index-design-system-with-builder.ts",
      note: "Figma, GitHub, या कोड से Builder डिज़ाइन-सिस्टम इंडेक्सिंग शुरू करता है",
    },
    {
      path: "app/routes/deck.$id.tsx",
      note: "डेक एडिटर",
    },
    {
      path: "app/routes/deck.$id_.present.tsx",
      note: "फ़ुल-स्क्रीन प्रेजेंटेशन मोड",
    },
    {
      path: "app/routes/agent.tsx",
      note: "फ़ुल-पेज Agent टैब",
    },
    {
      path: "app/routes/p.$id.tsx",
      note: "पब्लिक/एजेंट-रीडेबल सिंगल-डेक प्रीव्यू",
    },
    {
      path: "app/routes/share.$token.tsx",
      note: "पब्लिक रीड-ओनली स्नैपशॉट पेज",
    },
    {
      path: "app/components/editor/",
      note: "स्लाइड कैनवास, टूलबार, साइडबार, और पैनल",
    },
    {
      path: "server/db/schema.ts",
      note: "decks, slide_comments, deck_versions, design_systems, deck_share_links",
    },
    {
      path: ".agents/skills/create-deck/SKILL.md",
      note: "हर लेआउट के लिए एग्ज़ैक्ट स्लाइड HTML टेम्पलेट",
    },
    {
      path: ".agents/skills/creative-context/SKILL.md",
      note: "रीयूज़ लैडर और रिट्रीवल प्रोसीज़र",
    },
  ]}
/>

ज़्यादातर एडिटर UI कस्टमाइज़ेशन `app/components/editor/` के तहत होता है: स्लाइड कैनवास, टूलबार, साइडबार, बबल मेनू, स्लैश मेनू, और हिस्ट्री पैनल।

#### Skills — `templates/slides/.agents/skills/`

एजेंट skills जो पैटर्न एक्सप्लेन करते हैं जब एजेंट को कोड मॉडिफ़ाई करने की ज़रूरत होती है:

- `create-deck` — नया डेक कैसे बनाएं, हर लेआउट के लिए एग्ज़ैक्ट स्लाइड HTML टेम्पलेट के साथ
- `slide-editing` — अलग-अलग स्लाइड्स कैसे एडिट करें
- `deck-management` — डेक कैसे स्टोर और एक्सेस किए जाते हैं
- `design-systems` — डिज़ाइन-सिस्टम डेटा शेप और जेनरेट की गई स्लाइड्स पर टोकन कैसे लागू होते हैं
- `creative-context` — रीयूज़ लैडर, रिट्रीवल प्रोसीज़र, और Library सबमिशन

#### AGENTS.md

`templates/slides/AGENTS.md` वह छोटा राउटर है जिसे एजेंट हर कन्वर्सेशन पर पढ़ता है। यह `.agents/skills/` के तहत मौजूद skills की तरफ़ इशारा करता है और कोर नियम, पर्सिस्टेंस मॉडल, और application-state कॉन्ट्रैक्ट बताता है। जब भी आप कोई स्लाइड लेआउट या डिज़ाइन-सिस्टम सोर्स जोड़ें या बदलें, तो संबंधित skill को अपडेट करें।

## आगे क्या

- [**सुविधाएं**](/docs/template-slides-features) — यूज़र-फ़ेसिंग एडिटिंग, प्रज़ेंटिंग, शेयरिंग, डिज़ाइन-सिस्टम, और इम्पोर्ट/एक्सपोर्ट फ़्लो जिसे यह डेटा मॉडल पावर देता है
- [**एजेंट से बात करें**](/docs/template-slides-agent) — काम के हिसाब से प्रॉम्प्ट और एजेंट क्या देखता है
- [**मल्टी-ऐप वर्कस्पेस में स्लाइड**](/docs/template-slides-integrations) — ये actions स्लाइड के बाहर से कैसे कॉल किए जाते हैं
- [**साझाकरण**](/docs/sharing) — शेयर-ग्रांट मॉडल जिस पर `deck_shares` और `design_system_shares` बने हैं
- [**रीयल-टाइम कोलैबोरेशन**](/docs/real-time-collaboration) — Yjs पाइपलाइन जिसे डेक दूसरी कोलैबोरेटिव सतहों के साथ शेयर करते हैं
- [**स्लाइड ओवरव्यू**](/docs/template-slides) — ऐप समरी पर वापस जाएं
