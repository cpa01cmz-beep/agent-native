---
title: "剪辑：开发者指南"
description: "快速入门、完整的 action 清单、数据模型、路由，以及如何扩展剪辑模板。"
---

# 开发者指南

本页面适用于任何定制剪辑模板或对其进行扩展的人：快速入门、每一个 action、数据模型、路由界面，以及如何向其中添加内容。有关这个应用做什么，请参阅 [剪辑](/docs/template-clips)。

## 快速入门

<Steps>

### 创建应用

```bash
npx @agent-native/core@latest create my-clips --standalone --template clips
cd my-clips
pnpm install
```

这需要先安装 [Node.js](https://nodejs.org) 22.22 或更高版本，以及 [pnpm](https://pnpm.io)。如果你还没有安装它们中的任何一个，或者还没有配置好 LLM 连接（Builder.io、一个 Anthropic/OpenAI 密钥，或本地 Ollama），请参阅[快速入门](/docs/getting-started)。

### 启动开发服务器

```bash
pnpm dev
```

应用会自动在你的浏览器中打开。录制功能无需任何额外设置即可使用，因为浏览器或桌面应用是在本地进行捕获的。只有在需要文件存储或 AI 额度的 action 上，并且这两者都还没有配置好之前，你才会遇到错误。

</Steps>

<Callout tone="info">

录制、转录，以及 AI 标题/摘要，都需要先配置好文件存储和 AI 额度，才能端到端地正常工作。从**设置**中连接 Builder.io，即可一次性获得两者（它的免费套餐包含托管 AI 额度），或者分别连接 S3 兼容存储，并自带一个 Groq 密钥用于转录。参阅[转录和 AI 元数据](/docs/template-clips-features#transcription-and-ai-metadata)。

</Callout>

## Action 清单

每一个操作都是 `templates/clips/actions/` 中的一个 TypeScript 文件，自动挂载在 `POST /_agent-native/actions/:name` 上。

### 库和录制内容

| Action                                                                                    | 它的作用                                       |
| ----------------------------------------------------------------------------------------- | ---------------------------------------------- |
| `list-recordings`, `search-recordings`                                                    | 库、与我共享、回收站，以及按空间划分范围的列表 |
| `create-recording`, `finalize-recording`                                                  | 创建记录行；完成上传                           |
| `import-loom-recording`                                                                   | 导入一个 Loom 视频或一个直接的 MP4/WebM URL    |
| `update-recording`                                                                        | 标题、描述、密码、过期时间、可见性             |
| `move-recording`                                                                          | 把一个或多个录制内容移到某个文件夹或根目录     |
| `archive-recording`, `trash-recording`, `restore-recording`, `delete-recording-permanent` | 生命周期：隐藏、软删除、撤销，或永久删除       |
| `reprocess-recording`                                                                     | 修复无法跳转或卡死的媒体                       |
| `generate-filmstrip`                                                                      | 编辑器时间轴的帧缩略图条                       |
| `tag-recording`, `set-thumbnail`                                                          | 打标签，以及手动选择的缩略图帧                 |

### 文件夹和空间

| Action                                            | 它的作用                                     |
| ------------------------------------------------- | -------------------------------------------- |
| `create-folder`, `rename-folder`, `delete-folder` | 嵌套文件夹；删除一个会把其中的内容上移       |
| `create-space`, `rename-space`, `delete-space`    | 按组织划分范围、基于主题的录制内容容器       |
| `add-recording-to-space`                          | 在一个空间中添加或移除一段录制内容（多对多） |
| `add-space-member`, `remove-space-member`         | 空间级别的查看者、贡献者和管理员角色         |

### 编辑

| Action                                   | 它的作用                               |
| ---------------------------------------- | -------------------------------------- |
| `trim-recording`, `split-recording`      | 叠加一条剪切指令；从不触碰源媒体       |
| `remove-silences`, `remove-filler-words` | 由代理执行：读取转录文本，为你应用修剪 |
| `stitch-recordings`                      | 按顺序把多段录制内容合并成一段         |
| `undo-edit`, `clear-edits`               | 移除最近一次编辑，或重置为原始状态     |

### 转录和 AI 元数据

| Action                                                                          | 它的作用                                        |
| ------------------------------------------------------------------------------- | ----------------------------------------------- |
| `request-transcript`, `cleanup-transcript`                                      | 转录；`force`/`regenerate` 用于重新进行一次转录 |
| `regenerate-title`, `regenerate-summary`, `regenerate-chapters`, `set-chapters` | AI 元数据，或手动设置章节                       |
| `get-builder-credit-status`                                                     | Builder.io 的 AI 额度是否被暂停                 |

### 评论和表情反应

| Action                                                                                   | 它的作用                        |
| ---------------------------------------------------------------------------------------- | ------------------------------- |
| `add-comment`, `update-comment`, `delete-comment`, `reply-to-comment`, `resolve-comment` | 带线索的、带时间戳的评论        |
| `react-to-comment`, `react-to-recording`                                                 | 表情符号反应                    |
| `list-comments`, `list-recording-mention-members`                                        | 读取评论；查找可以被 @提及 的人 |

### 屏幕记忆（Rewind）

| Action                                                                                                                          | 它的作用                                         |
| ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ |
| `get-screen-memory-status`, `query-screen-memory-context`                                                                       | 读取本地 Rewind 缓冲区                           |
| `request-rewind-extension`, `get-rewind-extension-request`, `list-rewind-extension-requests`, `update-rewind-extension-request` | 把 Rewind 历史记录添加到一个剪辑的审核与批准流程 |
| `apply-rewind-extension`, `make-recording-private-for-rewind`                                                                   | 应用一个已批准的扩展；先强制将该剪辑设为私密     |

### 跨应用和洞察

| Action                                                                       | 它的作用                                                            |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| `export-to-brain`                                                            | 把准备好的转录文本发送到 Brain，支持回填和可续传的游标              |
| `prepare-crm-call-evidence`                                                  | 为 CRM 自动化提供一个不透明的剪辑 id，加上一个持久的 `/r/<id>` 链接 |
| `create-recording-agent-link`                                                | 为一段录制内容生成一个限定范围、有时效的 `agent_access` 链接        |
| `get-recording-insights`, `get-organization-insights`, `export-insights-csv` | 观看次数、互动情况，以及组织级 CSV 导出                             |
| `list-viewers`, `list-clip-views`                                            | 谁观看过，以及何时观看                                              |

### 组织

| Action                                                                                  | 它的作用                              |
| --------------------------------------------------------------------------------------- | ------------------------------------- |
| `create-organization`, `invite-member`, `accept-invite`, `decline-invite`, `get-invite` | 组织创建和邀请                        |
| `remove-member`, `update-member-role`                                                   | 管理成员身份和角色                    |
| `set-organization-branding`, `list-organization-state`                                  | 品牌颜色/徽标、默认可见性、组织级读取 |

### 导航、嵌入和缺陷报告

| Action                                                                                | 它的作用                                                         |
| ------------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `view-screen`, `navigate`, `refresh-list`                                             | 读取上下文；移动界面                                             |
| `get-playback-position`, `save-playback-position`, `get-recording-player-data`        | 恢复播放；完整的转录文本和诊断信息                               |
| `create-recording-intake-link`, `create-intake-recording`, `create-intake-agent-link` | 匿名的第三方嵌入流程。参阅[嵌入剪辑](/docs/template-clips-embed) |
| `save-bug-report-context`, `save-browser-diagnostics`, `save-browser-transcript`      | 嵌入式缺陷报告启动器和 Chrome 扩展程序诊断信息                   |

## 数据模型

定义在 `templates/clips/server/db/schema.ts` 中。

<Callout tone="warning">

团队、成员和邀请记录存放在框架自身的 `organizations`、`org_members` 和 `org_invitations` 表中，而不是一个剪辑专用的表中。`organization_settings` 是剪辑自己的附属表：品牌颜色、徽标和默认可见性，每个组织一行。一对更早的 `workspaces`/`workspace_members` 仍然存在，仅仅是为了让一次性的启动迁移能够在现有部署上回填框架表。不要针对它们构建新功能。

</Callout>

<DataModel
  id="doc-block-clips-data-model"
  title="剪辑数据模型"
  summary={
    "录制内容是核心资源。一段录制内容属于一个文件夹（或不属于任何文件夹），但可以同时位于任意数量的空间中。评论和分享都以录制内容的 id 作为关联键。"
  }
  entities={[
    {
      id: "recordings",
      name: "recordings",
      note: "核心资源（可归属）",
      fields: [
        { name: "id", type: "id", pk: true },
        { name: "organizationId", type: "id" },
        { name: "folderId", type: "id", nullable: true },
        {
          name: "spaceIds",
          type: "json",
          note: "空间 id 的 JSON 数组，多对多",
        },
        { name: "title", type: "string" },
        {
          name: "status",
          type: "enum",
          note: "uploading | processing | ready | failed",
        },
        { name: "durationMs", type: "int" },
        { name: "videoUrl", type: "string", nullable: true },
        {
          name: "editsJson",
          type: "json",
          note: "分层的、非破坏性编辑指令",
        },
        { name: "chaptersJson", type: "json" },
        { name: "password", type: "string", nullable: true },
        { name: "expiresAt", type: "datetime", nullable: true },
        { name: "archivedAt", type: "datetime", nullable: true },
        { name: "trashedAt", type: "datetime", nullable: true },
      ],
    },
    {
      id: "folders",
      name: "folders",
      note: "嵌套文件夹",
      fields: [
        { name: "id", type: "id", pk: true },
        { name: "organizationId", type: "id" },
        { name: "parentId", type: "id", nullable: true },
        {
          name: "spaceId",
          type: "id",
          nullable: true,
          note: "null 表示个人库",
        },
        { name: "ownerEmail", type: "string" },
        { name: "name", type: "string" },
      ],
    },
    {
      id: "spaces",
      name: "spaces",
      note: "按主题划分范围的组织子容器",
      fields: [
        { name: "id", type: "id", pk: true },
        { name: "organizationId", type: "id" },
        { name: "name", type: "string" },
        { name: "isAllCompany", type: "bool" },
      ],
    },
    {
      id: "space_members",
      name: "space_members",
      note: "每个空间的角色",
      fields: [
        { name: "id", type: "id", pk: true },
        { name: "spaceId", type: "id", fk: "spaces.id" },
        { name: "email", type: "string" },
        { name: "role", type: "enum", note: "viewer | contributor | admin" },
      ],
    },
    {
      id: "recording_comments",
      name: "recording_comments",
      note: "带线索的、带时间戳的评论",
      fields: [
        { name: "id", type: "id", pk: true },
        { name: "recordingId", type: "id", fk: "recordings.id" },
        { name: "threadId", type: "id" },
        { name: "parentId", type: "id", nullable: true },
        { name: "authorEmail", type: "string" },
        { name: "content", type: "string" },
        { name: "videoTimestampMs", type: "int" },
        { name: "resolved", type: "bool" },
      ],
    },
    {
      id: "recording_shares",
      name: "recording_shares",
      note: "共享授权，来自框架共享的通用能力",
      fields: [
        { name: "id", type: "id", pk: true },
        { name: "recordingId", type: "id", fk: "recordings.id" },
        { name: "principal", type: "string", note: "用户或组织" },
        { name: "role", type: "enum", note: "viewer | editor | admin" },
      ],
    },
  ]}
  relations={[
    { from: "folders", to: "recordings", kind: "1-n", label: "包含" },
    {
      from: "spaces",
      to: "recordings",
      kind: "n-n",
      label: "录制内容可以加入任意数量的空间",
    },
    { from: "spaces", to: "space_members", kind: "1-n", label: "拥有成员" },
    {
      from: "recordings",
      to: "recording_comments",
      kind: "1-n",
      label: "拥有评论",
    },
    {
      from: "recordings",
      to: "recording_shares",
      kind: "1-n",
      label: "拥有共享授权",
    },
  ]}
/>

## 逐步讲解一个 action

`create-recording-agent-link` 展示了值得在任何生成限定范围凭证的 action 中复用的模式：在交出任何东西之前先检查资源的真实状态，把实际的令牌生成工作委托给一个共享的框架辅助函数，而不是自己造轮子，并且从那一份授权中派生出调用者所需要的每一个 URL。

<AnnotatedCode
  id="doc-block-clips-agent-link"
  title="生成一个限定范围的 agent-access 链接"
  filename="templates/clips/actions/create-recording-agent-link.ts"
  language="ts"
  code={
    'import { defineAction } from "@agent-native/core/action";\nimport {\n  buildAgentAccessUrl,\n  createScopedAgentAccessGrant,\n  getRequestContext,\n  getRequestUserEmail,\n} from "@agent-native/core/server";\nimport { ForbiddenError, resolveAccess } from "@agent-native/core/sharing";\nimport { z } from "zod";\n\nimport {\n  CLIPS_AGENT_ACCESS_TTL_SECONDS,\n  getServerAppBasePath,\n} from "../server/lib/public-agent-context.js";\nimport {\n  buildAgentApiUrls,\n  CLIP_AGENT_ACCESS_TOKEN_PREFIX,\n} from "../shared/agent-context.js";\nimport { BUG_REPORT_AGENT_ACCESS_TTL_SECONDS } from "../shared/bug-report.js";\n\nfunction appOrigin(): string {\n  const origin =\n    getRequestContext()?.requestOrigin ||\n    process.env.APP_URL ||\n    process.env.BETTER_AUTH_URL ||\n    "http://localhost:3000";\n  try {\n    return new URL(origin).origin;\n  } catch {\n    return "http://localhost:3000";\n  }\n}\n\nexport default defineAction({\n  description:\n    "Create a temporary private agent-readable link for one Clips recording. The URL is scoped to that recording and expires after two hours.",\n  schema: z.object({\n    recordingId: z.string().describe("Recording ID"),\n    agentLabel: z\n      .string()\n      .trim()\n      .min(1)\n      .max(60)\n      .optional()\n      .describe(\n        "Name of the agent that will read this link (e.g. \'Fusion\', \'Claude Code\'). Recorded against every read the link produces, so the owner sees a name instead of an unidentified agent.",\n      ),\n    ttlSeconds: z\n      .number()\n      .int()\n      .positive()\n      .max(BUG_REPORT_AGENT_ACCESS_TTL_SECONDS)\n      .optional()\n      .describe(\n        "Optional scoped-link lifetime in seconds, capped at seven days. Defaults to two hours.",\n      ),\n  }),\n  readOnly: true,\n  run: async (args) => {\n    const access = await resolveAccess("recording", args.recordingId);\n    if (!access) {\n      throw new ForbiddenError(`No access to recording ${args.recordingId}`);\n    }\n\n    const recording = access.resource as {\n      id: string;\n      archivedAt?: string | null;\n      trashedAt?: string | null;\n    };\n    if (recording.archivedAt || recording.trashedAt) {\n      throw new ForbiddenError(\n        `Recording ${args.recordingId} is not shareable`,\n      );\n    }\n\n    const grant = createScopedAgentAccessGrant({\n      resourceKind: CLIP_AGENT_ACCESS_TOKEN_PREFIX,\n      resourceId: recording.id,\n      viewerEmail: getRequestUserEmail() || undefined,\n      agentLabel: args.agentLabel,\n      ttlSeconds: args.ttlSeconds ?? CLIPS_AGENT_ACCESS_TTL_SECONDS,\n    });\n    const origin = appOrigin();\n    const basePath = getServerAppBasePath();\n    const pageUrl = buildAgentAccessUrl({\n      path: `/share/${encodeURIComponent(recording.id)}`,\n      origin,\n      basePath,\n      token: grant.token,\n    });\n    const api = buildAgentApiUrls(recording.id, {\n      origin,\n      basePath,\n      token: grant.token,\n    });\n\n    return {\n      recordingId: recording.id,\n      url: pageUrl,\n      contextUrl: api.contextUrl,\n      expiresAt: grant.expiresAt,\n      ttlSeconds: grant.ttlSeconds,\n    };\n  },\n});\n'
  }
  annotations={[
    {
      lines: "60-74",
      label: "在生成任何东西之前，先检查资源状态",
      note: "resolveAccess() 会确认调用者究竟能否看到这段录制内容，然后一个显式的 archivedAt/trashedAt 检查会拒绝为一个不应该再被分享的剪辑发放链接，即便对方在技术上仍然拥有访问权限。",
    },
    {
      lines: "76-82",
      label: "令牌生成的工作被委托出去，而不是重新发明",
      note: "createScopedAgentAccessGrant() 是一个共享的框架辅助函数，因此剪辑自身不拥有任何令牌签名逻辑。TTL 的上限由 zod schema 限定，并且默认值来自一个命名常量，而不是一个魔法数字。",
    },
    {
      lines: "83-95",
      label: "调用者需要的每一个 URL 都来自同一份授权",
      note: "分享页面的 URL 和三个代理 API 的 URL（buildAgentApiUrls）都是从同一个令牌派生出来的，因此只有一个地方可能错误地泄露或重用一份授权，而不是三个地方。",
    },
    {
      lines: "79",
      label: "这个链接会记住是谁请求了它",
      note: "viewerEmail 和 agentLabel 会被记录在这份授权上，因此一段录制内容的所有者可以看到是哪个人、哪个代理读取过它，而不是一条匿名的访问日志记录。",
    },
  ]}
/>

## 路由参考

<FileTree
  id="doc-block-clips-routes"
  title="剪辑模板结构"
  entries={[
    {
      path: "actions/",
      note: "每一个可供代理调用的 action —— 录制内容、文件夹、空间、评论、分享、洞察",
    },
    {
      path: "app/routes/_app.library._index.tsx",
      note: "库的网格视图；_app.library.folder.$folderId.tsx 是单个文件夹",
    },
    { path: "app/routes/_app.shared.tsx", note: "与我共享" },
    {
      path: "app/routes/_app.spaces._index.tsx",
      note: "空间列表；_app.spaces.$spaceId.tsx 是单个空间",
    },
    {
      path: "app/routes/_app.r.$recordingId.tsx",
      note: "录制内容详情页面和编辑器",
    },
    { path: "app/routes/_app.agent.tsx", note: "全页代理聊天标签页" },
    {
      path: "app/routes/_app.settings._index.tsx",
      note: "设置，包括 Labs 标签页",
    },
    { path: "app/routes/record.tsx", note: "录制工具" },
    {
      path: "app/routes/share.$shareId.tsx",
      note: "公开分享页面；embed.$shareId.tsx 是 iframe 嵌入版本",
    },
    {
      path: "app/routes/download.tsx",
      note: "桌面应用下载页面",
    },
    {
      path: "server/db/schema.ts",
      note: "Drizzle 表 —— 录制内容、文件夹、空间、评论",
    },
    {
      path: "server/routes/api/agent-context.json.get.ts",
      note: "代理可读端点，此外还有 agent-transcript.json 和 agent-frame.jpg",
    },
    {
      path: "server/lib/google-calendar-client.ts",
      note: "Google Calendar API 客户端",
    },
    {
      path: "shared/api.ts",
      note: "服务器和客户端共用的 TypeScript 类型",
    },
    {
      path: "desktop/",
      note: "Tauri 桌面应用（屏幕/系统音频捕获、Rewind）",
    },
    {
      path: "chrome-extension/",
      note: "Chrome 扩展程序（录制加上浏览器诊断信息）",
    },
  ]}
/>

## 定制它

这个应用的每一部分都是可编辑的源码。从这里开始：

- `templates/clips/actions/` — 用 `defineAction` 添加一个新文件，把新的能力同时暴露给代理和前端。
- `templates/clips/app/routes/` — 界面，如上面列出的。
- `templates/clips/server/db/schema.ts` — 用 Drizzle 添加列或表。保持 schema 与 PostgreSQL 兼容，以便该模板既能在本地 PGlite 上运行，也能在托管的 Postgres 上运行。
- `templates/clips/server/lib/` — 提供方集成：`google-calendar-client.ts`、`s3-upload-provider.ts`、`slack-oauth.ts`、`video-remux.ts`、`video-filmstrip-sprite.ts`。
- `templates/clips/AGENTS.md` — 代理指令。当你教给代理新的能力或约定时，更新这个文件。
- `templates/clips/.agents/skills/` — 代理遵循的详细模式。相关的 skill：`recording`、`ai-video-tools`、`video-editing`、`video-sharing`、`screen-memory`。
- `templates/clips/shared/api.ts` — 服务器和客户端共用的 TypeScript 类型。
- `templates/clips/desktop/` 和 `templates/clips/chrome-extension/` — 原生捕获界面各自独立的软件包，各有自己的 `package.json` 和构建方式。

如果你添加一项功能，记住要更新全部四个方面：界面、action、skill 或 AGENTS.md 条目，以及代理需要看到的任何应用状态。这正是让代理和界面保持对等的关键。

## 接下来

- [**剪辑**](/docs/template-clips) — 概览及它取代了什么
- [**功能**](/docs/template-clips-features) — 这些 action 和这个数据模型所驱动的用户可见行为
- [**与 Agent 对话**](/docs/template-clips-agent) — 代理如何在这些 action 和不太常见的 action 之间做出选择
- [**多应用工作区中的剪辑**](/docs/template-clips-integrations) — 代理可读端点和 `clip.created` 事件如何被其他应用使用
- [**嵌入剪辑**](/docs/template-clips-embed) — 匿名的第三方录制接入流程
- [**Actions**](/docs/actions-overview) — 这个模板所构建于其上的 action 系统
- [**共享**](/docs/sharing) — `recording_shares` 所使用的共享授权模型
