---
title: "Definindo Actions"
description: "A anatomia de defineAction(): schema, run e configuração de HTTP, além de como manter a superfície de actions pequena."
---

# Definindo Actions

Toda action começa com uma chamada a `defineAction()`. Ela reúne uma description para o agente, um schema que valida a entrada, e uma função `run` que faz o trabalho, para que a UI e o agente chamem exatamente o mesmo código.

<AnnotatedCode
  id="doc-block-actions-defining-1"
  title="Anatomia de uma action"
  filename="actions/reply-to-email.ts"
  language="ts"
  code={
    'import { defineAction } from "@agent-native/core/action";\nimport { z } from "zod";\n\nimport { getDb, schema } from "../server/db/index.js";\n\nexport default defineAction({\n  description: "Reply to an email thread in the user\'s voice.",\n  schema: z.object({\n    emailId: z.string().describe("The id of the email to reply to."),\n    body: z.string().describe("The reply body, in markdown."),\n  }),\n  run: async ({ emailId, body }) => {\n    const db = getDb();\n    await db.insert(schema.replies).values({ emailId, body });\n    return { ok: true, emailId };\n  },\n});'
  }
  annotations={[
    {
      lines: "7",
      label: "Superfície de ferramenta",
      note: "`description` é o que o agente lê para decidir quando chamar isso. As chamadas `.describe()` por campo também entram no JSON Schema.",
    },
    {
      lines: "8-11",
      label: "Contrato tipado",
      note: "Um schema valida a entrada de **todas** as superfícies e é convertido para JSON Schema para o modelo. Entradas inválidas nunca chegam a `run`.",
    },
    {
      lines: "12-16",
      label: "Uma implementação",
      note: "O corpo `run` é a única fonte de verdade. O botão da UI e a ferramenta do agente executam exatamente isso. `getDb()` e `schema` vêm do `server/db/index.ts` do seu app. Veja [Database](/docs/server-database).",
    },
  ]}
/>

O framework descobre automaticamente cada arquivo em `actions/` e o monta na inicialização. Não há mais nada para registrar manualmente.

<Signature
  id="doc-block-actions-defining-2"
  name="defineAction"
  params={[
    {
      name: "config",
      type: "DefineActionConfig<TSchema, TReturn, TOutputSchema>",
      description:
        "Tudo abaixo é um único objeto de config. Apenas description, schema e run são obrigatórios.",
      fields: [
        {
          name: "description",
          type: "string",
          description:
            "O que o agente lê para decidir quando chamar esta action.",
        },
        {
          name: "schema",
          type: "StandardSchemaV1",
          description:
            "Valida a entrada em toda superfície. Veja Opções de schema abaixo.",
        },
        {
          name: "run",
          type: "(args, ctx?) => Promise<TReturn> | TReturn",
          description:
            "A única implementação que toda superfície chama. Veja Contexto de Execução para o argumento ctx.",
        },
        {
          name: "http",
          type: "ActionHttpConfig | false",
          optional: true,
          description:
            "Sobrescreve o método/path montado, ou desativa completamente o endpoint HTTP. Veja Configuração de HTTP abaixo.",
        },
        {
          name: "readOnly",
          type: "boolean",
          optional: true,
          default: "false",
          description:
            "Automaticamente true quando http.method é GET. Pula a atualização por polling da UI e a auditoria de escrita. Não é controle de acesso.",
        },
        {
          name: "outputSchema",
          type: "StandardSchemaV1",
          optional: true,
          description:
            "Valida o que run() retorna. Veja Validando o valor de retorno abaixo.",
        },
        {
          name: "outputErrorStrategy",
          type: '"warn" | "strict" | "fallback"',
          optional: true,
          default: '"warn"',
          description:
            "O que acontece quando há uma incompatibilidade na saída.",
        },
        {
          name: "outputFallback",
          type: "TReturn",
          optional: true,
          description:
            'Retornado no lugar de um resultado inválido quando outputErrorStrategy é "fallback".',
        },
        {
          name: "parallelSafe",
          type: "boolean",
          optional: true,
          description:
            "Permite que uma action de mutação rode simultaneamente com outras chamadas de ferramenta no mesmo turno.",
        },
        {
          name: "agentTool",
          type: "boolean",
          optional: true,
          default: "true",
          description:
            "Defina como false para ocultar de toda lista de ferramentas do agente. Veja Acesso e Autorização.",
        },
        {
          name: "mcpTool",
          type: "boolean",
          optional: true,
          default: "agentTool",
          description:
            "Herda `agentTool`. Defina false para ocultar de agentes externos por MCP e A2A, ou true para declarar participação no catálogo curado. Veja Acesso e Autorização.",
        },
        {
          name: "endsTurn",
          type: "boolean",
          optional: true,
          default: "false",
          description:
            "Defina true quando uma chamada bem-sucedida entrega o controle ao usuário (formulário de pergunta, diálogo de admissão); torne-a a primeira ou única chamada do turno. Oculta de MCP, WebMCP e A2A, a menos que mcpTool: true — um chamador externo não pode receber uma resposta dentro do app. Veja Agentes Externos.",
        },
        {
          name: "link",
          type: "(result) => { url, label }",
          optional: true,
          description:
            "Construtor de link profundo puro e síncrono, lido pelos hosts MCP e pelo app de desktop. Veja Outras Superfícies.",
        },
        {
          name: "mcpApp",
          type: "object",
          optional: true,
          description:
            "Incorpora a rota real do app inline em hosts MCP-App. Veja Outras Superfícies.",
        },
        {
          name: "deferLoading",
          type: "boolean",
          optional: true,
          description:
            "Defina false para manter a action na primeira lista de ferramentas do agente, ou true para carregá-la sob demanda pelo tool-search. Veja Mantenha a superfície de actions pequena abaixo.",
        },
        {
          name: "toolCallable",
          type: "boolean",
          optional: true,
          default: "true",
          description:
            "Defina como false para bloquear a ponte de iframe de extensão em sandbox. Veja Acesso e Autorização.",
        },
        {
          name: "publicAgent",
          type: "PublicAgentActionConfig",
          optional: true,
          description:
            "{ expose: true } adiciona a action às superfícies públicas de MCP/A2A/OpenAPI. Veja Acesso e Autorização.",
        },
        {
          name: "needsApproval",
          type: "boolean | ((args, ctx?) => boolean | Promise<boolean>)",
          optional: true,
          default: "false",
          description:
            "Pausa o loop do agente para aprovação humana antes de run(). Veja Human-in-the-Loop Approvals.",
        },
        {
          name: "authorize",
          type: "ActionAuthorize<TInput>",
          optional: true,
          description:
            "Função guarda que decide se este chamador pode executar a action. Veja Acesso e Autorização.",
        },
        {
          name: "audit",
          type: "ActionAuditConfig",
          optional: true,
          description:
            "Ajusta a captura automática de auditoria que toda action de mutação já recebe. Veja Audit Log.",
        },
        {
          name: "chatUI",
          type: "ActionChatUIConfig",
          optional: true,
          description:
            "Declara um renderizador de chat UI nativo para o resultado da action. Veja Outras Superfícies.",
        },
      ],
    },
  ]}
  returns={{
    type: "ActionDefinition<TInput, TReturn>",
    description:
      "A definição tipada da action. Exporte-a como o export padrão do arquivo.",
  }}
/>

## Opções de schema {#schemas}

`schema` aceita qualquer biblioteca compatível com [Standard Schema](https://standardschema.dev):

- **Zod** (v4): a escolha mais comum, com a melhor inferência de tipos e conversão automática para JSON Schema.
- **Valibot**: uma boa escolha quando o tamanho do bundle importa.
- **ArkType**: uma boa escolha se você preferir sua sintaxe.

O schema faz dois trabalhos. Ele se converte em JSON Schema para a definição de ferramenta da Claude API, e valida a entrada em tempo de execução antes de `run()` disparar. Entradas inválidas nunca chegam ao seu handler.

Declare valores de enum e defaults em `.describe()`, mantenha o nome do id principal idêntico em toda uma família de ferramentas (`id`, não uma mistura de `id`/`deckId`), e faça a descrição de uma linha ser suficiente para chamar a ferramenta a partir de um catálogo compacto — a skill `actions` incluída em todo app traz a checklist.

## Validando o valor de retorno {#output-schema}

`schema` valida _entradas_. Para também validar o que uma action **retorna**, passe um `outputSchema` (qualquer schema compatível com Standard Schema: Zod, Valibot, ArkType, a mesma superfície de `schema`). O framework valida a entrada antes de `run()` e valida o resultado depois que `run()` resolve.

```ts
export default defineAction({
  description: "Summarize a thread.",
  schema: z.object({ threadId: z.string() }),
  outputSchema: z.object({
    summary: z.string(),
    messageCount: z.number(),
  }),
  outputErrorStrategy: "warn", // default
  run: async ({ threadId }) => {
    /* ...returns { summary, messageCount } ... */
  },
});
```

`outputErrorStrategy` controla o que acontece em uma incompatibilidade:

| Estratégia   | Comportamento na incompatibilidade                                                                                       |
| ------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `"warn"`     | **Padrão.** Executa `console.warn` com os problemas e retorna o resultado **original** sem alterações. Não é disruptivo. |
| `"strict"`   | Lança um erro claro, para que um bug na action seja fácil de perceber.                                                   |
| `"fallback"` | Retorna o valor `outputFallback` fornecido no lugar do resultado inválido.                                               |

Quando `run()` tem sucesso, o framework retorna o valor validado, então quaisquer defaults ou coerções em `outputSchema` entram em vigor, da mesma forma que acontece para a entrada. Quando você não passa um `outputSchema`, o valor de retorno permanece inalterado.

## Contratos de resultado {#result-contracts}

Um resultado de create ou update é lido por mais do que quem o chamou. Retorne `id` (para que `link` possa construir um deep link para a view de edição), uma `message` de uma linha e — quando a operação é um passo de um fluxo que o agente deve continuar conduzindo — `nextRequiredAction` nomeando a próxima ferramenta. Nunca formule como se estivesse esperando o usuário: chamadores MCP/WebMCP/A2A não podem receber uma resposta dentro do app. O loop do agente mantém `nextRequiredAction` nos prompts de continuação mesmo quando um resultado grande é truncado, e chamadores MCP o veem como `Next: …`.

## Configuração de HTTP {#http}

Por padrão, toda action é exposta como `POST /_agent-native/actions/<name>`. Sobrescreva com a opção `http`:

```ts
export default defineAction({
  description: "Get details for a lead.",
  schema: z.object({ leadId: z.string() }),
  http: { method: "GET" },
  run: async ({ leadId }) => {
    return await db.select().from(leads).where(eq(leads.id, leadId));
  },
});
```

Para uma action `GET`, `leadId` é passado como um query param: `/_agent-native/actions/get-lead?leadId=abc`.

<Endpoint id="doc-block-actions-defining-3" title="O endpoint de action auto-montado" method="GET" path="/_agent-native/actions/get-lead" summary={"Toda action é montada aqui automaticamente. O nome do arquivo é o nome da action."} auth={"Cookie de sessão (chamadas do frontend carregam `X-Agent-Native-Frontend: 1`), ou `Authorization: Bearer <token>` para chamadores externos"} params={[
  {
    "name": "leadId",
    "in": "query",
    "type": "string",
    "required": true,
    "description": "Argumentos de GET chegam como query params. Argumentos de POST chegam no corpo JSON."
  }
]} responses={[
  {
    "status": "200",
    "description": "O valor de retorno da action como JSON."
  },
  {
    "status": "400",
    "description": "A entrada falhou na validação de schema antes de run() disparar."
  }
]}>

POST é o padrão, e `http: { method: "GET" }` faz dele um GET. Os ganchos do React e `callAction()` sempre chamam esse caminho pelo nome, independentemente de qualquer override de `http.path`. Sistemas externos podem chamar a mesma rota com um bearer token de longa duração. Veja [HTTP API](/docs/http-api).

</Endpoint>

- **`http: { method: "GET" | "POST" | "PUT" | "DELETE" }`**: o padrão é `POST`. Actions `GET` são automaticamente marcadas como `readOnly`, então uma chamada bem-sucedida não dispara uma atualização por polling da UI.
- **`http: { path: "..." }`**: sobrescreve a URL montada sob `/_agent-native/actions/`. O padrão é o nome do arquivo. Esse override só muda a URL para chamadores HTTP diretos. `useActionQuery()`, `useActionMutation()` e `callAction()` sempre chamam `/_agent-native/actions/<name>`, independentemente desse override, então sobrescrever o path faz esses ganchos retornarem 404. Use um override de path apenas para chamadores HTTP externos. Note também que segmentos de rota `:param` no path sobrescrito não são parseados para os args de `run()`. Apenas query-string params e campos do corpo JSON são.
- **`http: false`**: desativa completamente o endpoint HTTP. Apenas o agente e a CLI podem chamar a action.
- **`readOnly: true`**: pula explicitamente a atualização por polling, mesmo para actions `POST` que não mutam nada. Isso não é controle de acesso. Não restringe quem pode chamar a action, e não afeta `useActionQuery()`, `useActionMutation()` ou qualquer gancho de cliente. Seus únicos efeitos são suprimir o evento de atualização pós-chamada e tirar a action da auditoria de escrita. Para ocultar uma action do modelo, use `agentTool: false`. Para restringir quem pode chamá-la, use as opções de auth e acesso em [Acesso e Autorização](/docs/actions-access-control).
- **`parallelSafe: true`**: permite que uma action de mutação rode ao mesmo tempo que outras chamadas de ferramenta no mesmo turno. Só defina isso quando a action for segura para rodar de forma concorrente e não depender de ordem. Actions de mutação são serializadas por padrão.

## Falhar uma action {#failing}

Uma action sinaliza falha lançando. Retornar `{ error: "..." }` parece sucesso para todas as partes do framework, então os disjuntores de retentativa do agente nunca contam isso e a chamada é registrada como um efeito colateral concluído.

O que você lança decide quanto da mensagem sobrevive. `fail()` lança um `ActionContractError`, e esse tipo é a única razão pela qual o texto chega a quem chamou. Um simples `throw new Error("No such email")` é indistinguível de uma falha do Postgres ou de um serviço externo, então a rota de action o substitui por um 500 genérico `"Internal server error"`, mantém a mensagem real no log do servidor e a reporta ao rastreamento de erros. Guarde o throw puro para falhas internas de verdade. É para isso que o 500 serve.

`fail()` aceita uma mensagem e um objeto de opções:

- **message**: o texto que quem chamou lê. Escreva para uma pessoa, não para um log.
- **options.errorCode**: um código estável e legível por máquina. Padrão: `"action_failed"`.
- **options.statusCode**: o status HTTP da rota de action. Padrão: `400`.
- **options.details**: contexto estruturado e seguro para quem chamou. Nunca coloque segredos ou saída bruta de driver aqui.

A chamada mais simples precisa apenas de uma mensagem:

```ts
import { defineAction, fail } from "@agent-native/core/action";
import { eq } from "drizzle-orm";
import { z } from "zod";

import { getDb, schema } from "../server/db/index.js";

export default defineAction({
  description: "Reply to an email thread in the user's voice.",
  schema: z.object({
    emailId: z.string().describe("The id of the email to reply to."),
    body: z.string().describe("The reply body, in markdown."),
  }),
  run: async ({ emailId, body }) => {
    const db = getDb();
    const [email] = await db
      .select()
      .from(schema.emails)
      .where(eq(schema.emails.id, emailId));
    if (!email) fail("That email is no longer in the mailbox.");
    await db.insert(schema.replies).values({ emailId, body });
    return { ok: true, emailId };
  },
});
```

Nomear a causa é o que permite à UI reagir a uma falha em vez de apenas exibi-la. O código e o status sobrevivem ao transporte:

```ts
if (!email) {
  fail("That email is no longer in the mailbox.", {
    errorCode: "email_not_found",
    statusCode: 404,
  });
}
```

O navegador recebe `{ error, errorCode, details }`. `useActionQuery()`, `useActionMutation()` e `callAction()` expõem isso como um `Error` carregando `status`, `errorCode`, `details` e `actionMessage`.

`error.message` mantém um prefixo `Action <name> failed:`, que cabe em um console e soa errado em um toast. `actionErrorMessage()` devolve apenas o texto que a action escreveu, ou `undefined` quando ninguém redigiu um:

```tsx
import {
  actionErrorMessage,
  useActionMutation,
} from "@agent-native/core/client/hooks";

const reply = useActionMutation("reply-to-email", {
  onError: (error) =>
    toast.error(actionErrorMessage(error) ?? t("email.replyFailed")),
});
```

Uma queda de rede, uma página de erro HTML de um proxy e uma simples linha de status não redigem mensagem alguma. Por isso devolvem `undefined` em vez de uma string que a UI mostraria por engano. Combine sempre com o seu próprio texto.

### O que cada lançamento produz {#failure-shapes}

| Lançado de `run()`                              | Status HTTP      | Corpo da resposta                            | Reportado ao rastreamento de erros |
| ----------------------------------------------- | ---------------- | -------------------------------------------- | ---------------------------------- |
| `fail(msg)`                                     | 400              | `{ error: msg, errorCode: "action_failed" }` | Não                                |
| `fail(msg, { errorCode, statusCode, details })` | seu `statusCode` | `{ error, errorCode, details }`              | Não                                |
| `new ActionContractError(msg, { errorCode })`   | 409              | `{ error, errorCode, details }`              | Não                                |
| `new AgentActionStopError(msg)`                 | 500              | `{ error: msg }`                             | Não                                |
| `new Error(msg)`                                | 500              | `{ error: "Internal server error" }`         | Sim                                |
| Entrada que falha no `schema`                   | 400              | `{ error: "Invalid action parameters..." }`  | Não                                |

`AgentActionStopError` é a que se lança quando o agente deve encerrar o turno em vez de ler a falha e tentar de novo. `ActionContractError` é o que `fail()` lança por você, então use-a diretamente apenas quando 409 for o status desejado.

### Escolha um status sobre o qual a política de retentativa possa agir {#failure-status}

`useActionQuery()` repete `429`, `502`, `503` e `504`, até três vezes, esperando 500 ms, depois 1 s, depois 2 s. Qualquer outro status é tratado como determinístico e aparece na primeira resposta. Reenviar uma requisição recusada por estar errada só compra a mesma recusa de novo. `useActionMutation()` não repete de forma alguma.

Então o status não é cosmético. Uma recusa enviada como `404` custa uma ida e volta. A mesma recusa enviada como `500`, ou lançada pura, custa quatro idas e voltas e quatro relatórios de erro duplicados.

Chamadores do agente não veem o status. O agente executa `run()` em processo, então recebe a mensagem diretamente, seja qual for o tipo, como `Error running <action>: <message>`. Um `errorCode` diferente do padrão `action_failed` é anexado como `(errorCode: not_found)`, para o modelo poder ramificar sem analisar a prosa. Agentes externos por MCP recebem o mesmo texto. Segredos na mensagem são removidos antes, e os disjuntores de erro repetido do agente limitam quantas vezes ele tenta de novo.

## Mantenha a superfície de actions pequena {#small-surface}

Toda action que o agente pode ver ocupa um slot na lista de ferramentas do modelo, e uma lista longa e sobreposta dificulta para o modelo escolher a ferramenta certa. Projete a superfície de actions como uma API que você mantém, não uma action por botão na UI:

- Prefira **uma única action `update` no estilo CRUD** que receba um patch de campos opcionais, em vez de muitas actions por campo como `update-name`, `update-order` e `update-color`. O chamador só envia os campos que mudaram.
- Antes de adicionar uma nova action de leitura para cada consulta ou filtro, considere primeiro uma opção mais geral: o [trio de API de provedor](/docs/template-dispatch-developers#calling-a-provider-directly) (`provider-api-catalog`, `provider-api-docs` e `provider-api-request`) para dados de provedor, ou a ferramenta dev `db-query` para dados do app.
- Marque actions apenas de UI ou programáticas como [`agentTool: false`](/docs/actions-access-control#agent-tool). Elas continuam chamáveis a partir do frontend ou por HTTP, sem ocupar um slot na lista de ferramentas do modelo.
- Marque com `deferLoading: false` o punhado de actions que o agente usa o tempo todo. São esses os schemas enviados na primeira requisição ao modelo; todo o resto carrega sob demanda pelo `tool-search` quando o agente precisa. Assim que uma action se exclui desse jeito, todas as não marcadas passam a ser adiadas, então marque o conjunto inicial inteiro de uma vez em vez de uma action por vez. Isso é sobre quanto custa o primeiro turno, não sobre acesso: uma action adiada é chamável assim que o `tool-search` a retorna. É a forma por action do array `initialToolNames` do plugin, e um app pode usar qualquer uma das duas.
- Exclua ou oculte actions que a UI não usa mais, em vez de deixá-las expostas ao modelo.

O repositório inclui um helper consultivo, `node scripts/audit-template-actions.mjs [template ...]` (alias `pnpm actions:audit`). Ele varre a pasta `actions/` de um template e sinaliza actions que a UI pode não estar mais usando, além de grupos de actions por campo que poderiam ser combinadas em uma só. Ele sempre sai com código 0 e nunca falha o CI, e suas heurísticas são conservadoras, então trate a saída dele como uma sugestão, não um erro.

## O que vem a seguir

- [**Visão geral**](/docs/actions-overview) — o que é uma action e o passo a passo hello-world
- [**Acesso e Autorização**](/docs/actions-access-control) — `agentTool`, `toolCallable`, `authorize` e o escopo de `ctx`
- [**Contexto de Execução**](/docs/actions-run-context) — a tabela completa do campo `ctx`
- [**Outras Superfícies**](/docs/actions-other-surfaces) — UI de chat nativa, CLI, A2A, MCP e actions padrão
- [**Database**](/docs/server-database) — `getDb()`/`schema` usados dentro de `run()`
- [**Dados e sincronização**](/docs/client-data) — chamando essa action a partir da UI
